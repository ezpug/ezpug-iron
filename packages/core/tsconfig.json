{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // core is isomorphic (Node, a browser widget, a worker): the timer/crypto
    // globals it touches exist in both, so it takes the DOM lib and never
    // @types/node.
    "lib": ["ES2023", "DOM", "DOM.Iterable"]
  },
  "include": ["src"]
}
