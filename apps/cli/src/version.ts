/**
 * The command's version, as `ezpug-iron --version` prints it. A constant
 * rather than a read of `package.json`, because the bundle the image runs
 * has none beside it; `version.test.ts` holds the two together.
 */
export const CLI_VERSION = '0.1.0'
