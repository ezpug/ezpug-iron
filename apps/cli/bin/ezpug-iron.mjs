#!/usr/bin/env node
// The `ezpug-iron` command (PRD-02 T33). The bundle `pnpm build` writes is the
// program; this file only exists so `pnpm iron`, the image's PATH and a global
// install have one stable name to call.
import '../dist/main.mjs'
