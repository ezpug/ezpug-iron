T1: the wire protocols in two languages. packages/protocol (tag internal, tsdown to dist/index.mjs only so plain-Node scripts can import the Zod): the server link at /link — up: hello (server token, plugin/SDK/CSS/MatchZy/Metamod versions, capabilities, installed plugin folders, hostname, map, state, matchId?, lastSeq), heartbeat, state (booting|idle|assigned|live|ending|draining), events (batch ≤200 of {seq, event} with a per-server link seq distinct from the vocabulary's per-match hint), command_result (applied|rejected + the Match API error code), backup ({mapNumber, roundNumber, filename, content ≤256 KiB}), console (uptime-stamped tail ≤500, on demand), player_command_result (closed refusal set: cooldown, no_charges, unknown_command, invalid_args, not_in_match, not_alive, refused); down: welcome (provider + serverId = the event source, heartbeatIntervalMs, ackedSeq), assign (matchId, game, the manifest resolved minus maps/widget, plugins, cfg, cvars merged flat, matchzyConfig? opaque for T9, maps, rules?, teams with profiles+loadouts, warmupLines, branding, demoUploadUrl?, restore?), command (a Match API MatchCommand verbatim minus sim.*, plus the link-only console), player_command, profile, release, drain, ack (accepted|duplicate|ephemeral|rejected — the platform's ingestion vocabulary plus rejected so a malformed event is never retried). The node link at /node — up: hello (enrolment|node token, version, region, lan, labels, capacity {maxInstances, warm}, sha256 image digest, instances snapshot), heartbeat, instances; down: welcome (nodeToken once, after an enrolment hello), start (instance spec: id, purpose warm|match, image, serverId, serverToken, ports, env, matchId?), stop, drain, undrain; nothing acked, instances is a snapshot. Constants (PROTOCOL_VERSION 1, paths, HEARTBEAT 10 s, HELLO_TIMEOUT 10 s, batch/backup/console limits, LINK_CLOSE_CODES 4001–4012) ride into the JSON Schema as x-constants and into C# as ProtocolConstants. Codegen decision: NJsonSchema 11.6 and quicktype-core 26 were both run on the real GameserverEvent schema — NJsonSchema keeps only the first oneOf branch (even with a discriminator hint), quicktype merges all 22 into one class of optionals with invented names, and neither distinguishes an absent optional from an explicit null — so neither could round-trip the goldens byte for byte; scripts/protocol-codegen.mjs is our own ~400-line generator over the exported JSON Schema (records in schema order with JsonPropertyOrder, required/init, optional → WhenWritingNull, nullable → T?, optional+nullable refused, defaults as initializers, consts get-only, enums with JsonStringEnumMemberName, an abstract record + dispatching converter per union registered on ProtocolJson.Options, doubles written like JavaScript so 0.000001 stays 0.000001); `pnpm lint` runs it with --check, `pnpm build` writes it. JSON Schema export is z.toJSONSchema over a registry in output mode (defaults filled = the bytes the producing side writes), names decided once in src/schema.ts; only the server link has a C# side (the node agent is TS). Fixtures: fixtures/frames/{server,orchestrator,commands,node,orchestrator-node}.json, one frame per type, tokens carry `not-a-secret`; 27 Vitest tests; EZPug.Sdk.Tests (34 xunit) round-trips every frame file and every vocabulary event in the spine's eleven recorded goldens byte for byte through the generated types — except the stream frames in stream-hello.json, whose payloads the fake's hub wrote type-first rather than in parse order, compared by value and said so in the test. pnpm verify green (TS + C#); verify:extended not demanded for T1. — 8a36d37 — 2026-09-05
T2: the orchestrator, standing. apps/orchestrator (tag app): Hono on 3430 via @hono/node-server, Drizzle over postgres.js (5443), ioredis (6383), the versions the platform runs (catalog + docs/pins.md rows, check-pins grows three cases). The composition root opens the pool and Redis, builds the app from the Match API route table (one handler per route, generated — authenticate by SHA-256 lookup → scope from the route's own declaration → parse → handle → response parsed as the orchestrator's own conformance guard), creates the server the links will attach to (T6) and listens last; main.ts pings both rails before the port opens. /healthz needs no key: database, redis, providers (empty until T3/T4), each bounded to 2 s on the clock, 503 while draining. Served now: gamemodes (the catalog) and the four keys routes from the spine's table — T5 keeps rotate, the budget patch and enforcement; every other route exists, authenticates, gates and validates, then answers `internal` naming the task that serves it (T3/T5/T12/T17/T20/T24), never a 404 that lies about the contract. Keys: `ezik_` + 43 base64url chars from the CSPRNG (one token grammar for api/server/node/enrolment/player kinds, TOKEN_PREFIXES is what a fixture scrub greps), only the hash stored, shown once, name unique among live keys (partial index), lastUsedAt touched once a minute; a KeyStore contract with a Postgres and a memory implementation so the HTTP rails are proven without a database. Rate limit: a token bucket per presented key's hash (strangers share one), in memory on the clock, 429 + Retry-After; burst 120 / 10 per s from env. Request log: method, path without query string, status, duration, the key's 12-char prefix, x-request-id (echoed when plausible, minted otherwise, also in every error envelope's details), every line through a redactor for our token prefixes. The schema, designed once, one migration (0000_iron-schema): api_keys (+ api_key_webhook_secrets, in clear because they sign, said so in the doc; fleet webhook columns for T31 now), matches (request whole + hash for the clientMatchId conflict, state_changed_at so deadlines re-arm after a restart, webhooks_stopped_at for a 410), match_events ((match, seq) PK, delivery_id unique as a column constraint — a uniqueIndex is emitted after the FK that needs it and the first migration failed on exactly that), webhook_deliveries, match_commands (idempotent correlationId across a restart), servers (the ledger: no password ever — join password is the match's, RCON is the provider's; link facts, GSLT lease, rcon_audit jsonb), server_tokens, nodes, node_enrolments, gslt_tokens (login token in clear, said so), backups, player_tokens; ids from the application, timestamptz from the clock; the platform's additive-safe guard ported and run over every committed migration in pnpm verify. Shutdown: the platform's createShutdown/createHttpDrain ported (8 s budget on the clock, a failing step logged and skipped, a second signal exits), steps health → listener → requests → redis → database with the slots T3/T6/T12 insert into named in shutdown-steps.ts. Dev world: compose.yaml (ezpug-iron-dev, postgres:17-alpine + init creating ezpug_iron_test, redis:8-alpine, loopback only), scripts/dev-env.sh (up copies .env.example, waits healthy, migrates both databases; reset asks before dropping a volume), root pnpm dev|dev:up|down|status|logs|reset|db:generate|db:migrate; keys:mint prints the first key once; verify:extended now runs dev:up and makes the database suites required. Tests: 81 in the package — config, tokens, log redaction, rate limiter, health, shutdown order/timeout/signals, the guard, the key service on the memory store, the app in-process (every route of the table registered, scopes, envelopes, 429, log without secrets), the Postgres key store and the rig inside rollback transactions, and the standing suite: the whole thing over a real socket with the published client, a key minted and used, the drain in order, the port gone; database suites skip with a printed reason when the world is down. Checked by hand on this box: tsx src/main.ts boots against the dev world, /healthz 200, SIGTERM drains in 30 ms in the documented order. docs/operations.md starts (env, dev world, health, boot/drain order, keys/scopes/limits/logs, the schema table, what is held in clear and why, the migration workflow and the guard). pnpm verify and pnpm verify:extended green. — 311d79c — 2026-09-05
T3: providers, the match machine, webhooks and the stream. apps/orchestrator/src/providers: the provider interface ported from the platform and grown (rcon, console, restore, an hourly price, allocate carries the ledger row id and the offering, configure carries the whole request + manifest + join password + the link's url and server token, an optional `sim` verb for Match.sim), the registry (drain/undrain and the last observation per provider → ProviderHealth), selection (filter on game/region/lan/workshopMaps/capacity; `provider` → that one, `simulated` → sim, else every non-sim provider and the sim only when none is registered; drained providers excluded; lanFirst when `lan` is asked, cheapestSuitable otherwise; failures reported and observed), the reaper (rows past expires_at → ttl_expired or deallocated; provider list() vs open rows → orphans after a 2 min grace deallocated + fleet.orphan_found to their match, lost rows surfaced to the machine's probe; failed listings reap nothing). The link seam (link/channels.ts): ServerEventSink (ingest → accepted|duplicate|ephemeral|rejected) and a LinkRegistry of ServerChannels keyed provider/serverId — the sim's in-process channel today, T6's socket tomorrow, the machine cannot tell. The sim provider is registered in T3 (the gate needs one; T4 keeps sim.* commands, chaos/kill/restore, the image and the dev contract): sim-N handles, .invalid addresses, roster invented for open-join from a seeded PRNG, the engine's events into the sink, pause/unpause/kick answered by *reporting* the event the plugin would (never awaited — awaiting the ingest inside the command step that holds the chain deadlocks, found the hard way). The machine (match/machine.ts): a MatchStore contract with a memory and a Postgres implementation (one contract test both pass), everything per match on that match's chain, deadlines armed from state_changed_at on the clock (allocate 2 min, boot 5 min, join 20 min → ttl_expired, recovery 5 min, ttl, a loss detector at 3 heartbeat intervals that probes the provider; resume() re-arms all of them after a restart and restarts dead walks), the door's refusal order (replay/conflict, secret id, sim scenario, unknown_gamemode, capability → no_capable_server / provider_unavailable, game_unsupported, map_not_allowed, the lifetime ceiling → budget_exceeded — the one budget check pulled forward from T5 so the budget-refused flow runs; concurrent/monthly/thresholds stay T5's), the walk (row before allocate, token hash into server_tokens, connect persisted at configure and shown from ready, next candidate on any failure, exhaustion → failed allocation_failed "no_capable_server"), events deduped per server seq, emit = match_events row + stream frame + webhook_deliveries row + kick, server_ready/going_live/player_connected/disconnected/series_end transitions, commands idempotent on correlationId across a restart and relayed down the channel (sim.* → command_unsupported until T4, restore → no_backup/T14, rcon needs admin), cancel before live, a lost server → match.recovering with the newest backup's round → failed server_lost when there is none (the resume onto a new server is T14's; the fake clock crash test covers the honest path). The webhook worker (webhooks/worker.ts): signWebhook with the key's secret (a rotated-away secret signs with nothing rather than a guess), the three headers, attempt timeout on the clock, the published schedule, given_up after ten, 410 → stopped + webhooks_stopped_at and nothing later is queued; kick on write + a 5 s sweep; per-match seq order, matches side by side. The stream hub (stream/hub.ts) with a Fanout seam — local for tests, Redis pub/sub (ezpug-iron:stream, a pub and a sub connection) in the process — presence snapshots, command_result frames, ticks batched per clock tick; the upgrade router on the raw server before Hono (stream/upgrade.ts; T6/T12 add /link and /node), bearer or ?token= (verified against player_tokens now, minted by T24), streamAllowedOrigins, hello then 4000 on a terminal match, 4008 past a megabyte buffered. The fleet service: servers, ledger (offset cursors), release by row id or handle (row released, match failed provider_error), providers health, drain/undrain, capacity from offerings. Every spine route served except mintPlayerToken (T24), console/rcon (T20), nodes (T12), budget (T5), gslt (T17). Composition: start() joins the fan-out, resumes open matches, arms the worker and the reaper; drain order health → listener → streams → requests → reaper → webhooks → matches → hub → redis → database. Tests: the conformance suite in process over memory + fake clock (9 flows pass, player-command/crash-restore/crash-lost skipped with the runner's reason until T24/T14), the machine suite (walk, refusals, deadlines, restart re-arm, commands, cancel, fleet, reaper, webhook schedule/410/given-up, stream), selection, the store contract over memory and Postgres, and conformance.extended.test.ts — the round's first extended-tier gate: the real composition on the system clock over Postgres and Redis on a real port, the sim at 60× (600× ends the match before the flow's commands land), webhooks to a real endpoint verified with the published verifier, the stream over a real ws upgrade through the Redis fan-out, no server left, no error line — 9 passed, 3 skipped, ~60 s. docs/operations.md gains "The match machine" and the new boot/drain order. pnpm verify green (TS + C#), pnpm verify:extended green. — 963cf9b — 2026-09-05
T4: the sim provider and the dev image. The sim.* family, end to end: providers/sim/channel.ts answers sim.step (step mode only; the beat it dealt comes back as `stepped`, `null` when the story is dry), sim.mode, sim.speed, sim.chaos and sim.kill, each carrying the simulator's own status back the way the published fake does — so a client that drives the fake drives the real service unchanged; the machine refuses the family with `command_unsupported` naming the provider whenever the match is not on `sim`, before a channel is asked, and the guard is `isSimCommand` from the contract rather than a `startsWith`. sim.kill tells nobody, on purpose: the loss detector notices the heartbeats stopped, probes, and opens the recovery window itself (a test kills a live match's box and watches it fail `server_lost` with the ledger row closed and no server left behind). The story's seed moved from `<root>#<matchId>#<serverId>` to `<root>#<matchId>`: two servers for one match now tell the same story, which is the whole reason the provider's new `restore` verb means anything — configure a replacement with the dead one's match id, `restore` it to a round backup, start it, and it boots, reconnects, restores and plays the match out (T14 walks that path through the machine; the verb, its determinism and its refusals are proven here off the machine, on a fake clock). The image: docker/orchestrator/Dockerfile (bookworm-slim on the Node the repo pins — a new docs/pins.md row with a check-pins case reading the FROM line; the workspace installed and built once, `pnpm deploy --legacy --prod` for the runtime tree, the internal packages bundled into dist/main.mjs by a new tsdown config beside a dist/migrate.mjs for T35, USER node, EXPOSE 3430, a HEALTHCHECK on /healthz through Node's own fetch because the image has no curl, the commit as a label) plus .dockerignore and `pnpm image:build` for the local `:dev` tag. The dev contract other projects' compose relies on is a table in docs/operations.md under "The image, and running it inside another project's dev world": EZPUG_IRON_PUBLIC_URL (a new name for the base URL, winning over EZPUG_IRON_BASE_URL), EZPUG_IRON_PROVIDERS=sim, EZPUG_IRON_MIGRATE_ON_BOOT + EZPUG_IRON_MIGRATIONS_DIR (both set in the image: it migrates before the port opens, advisory-locked and idempotent) and EZPUG_IRON_BOOTSTRAP_API_KEY — the consumer decides the secret because a minted key is shown once and a compose file has nobody to show it to; keys.adopt writes the row for a given secret, ensureBootstrapKey is the policy (name `bootstrap`, all three scopes, a dev budget, idempotent across restarts, the previous key *revoked* when the value changes), and the config refuses the variable under NODE_ENV=production with a message that names the way out — the image defaults to production, so a dev world sets NODE_ENV=development. src/image.test.ts holds the Dockerfile and the doc against each other so the contract cannot drift unwatched. **P1 fixed, not retried into green:** the extended conformance flaked once on webhook-replay (66 envelopes, Match.seq 65) — `end()` committed the terminal state and *then* appended `match.ended`, and `matches.get` reads off the match's chain, so a poll between the two saw a terminal match whose seq was one behind its own last event and the events route held one more. `appendEvent` now takes the state patch and applies it in the same transaction; the terminal state and the fact that announces it are one write. Three consecutive extended runs green afterwards. Checked by hand on this box: `pnpm image:build`, the container on the host network against the dev world — migrations applied, the bootstrap key adopted (and the same key id on restart), /healthz 200 with the sim provider, docker health `healthy`, SIGTERM drained in the documented order — then a whole match driven through the container over HTTP with the bootstrap key: a webhook secret registered on it through the admin scope, three `sim.step`s (server_ready, two player_connected), `sim.speed 600`, `sim.mode auto`, ended `completed` with 85 events, the last event's seq equal to the match's, no open ledger row; the container, its key and its rows removed afterwards. pnpm verify and pnpm verify:extended green (137 passed / 6 skipped in the orchestrator, the conformance suite against the real service 9 passed / 3 skipped — player-command, crash-restore and crash-lost still skip with their reason until T24 and T14). — 570b7fe — 2026-09-05
T5: budgets and keys. apps/orchestrator/src/budget/service.ts: the three ceilings enforced against the ledger, never the caller — maxConcurrentServers from the open rows, maxServerLifetimeMinutes from the request, monthlyCents from the month's spend (cost_hourly_cents x each row's open time, live rows accruing to now, floored to a cent, the UTC calendar month) — checked at the door after capability, priced at what the walk's *first* candidate charges, so the refusal names what this match would actually cost; 402 budget_exceeded with details.limit, before a ledger row exists, so a refused request leaves nothing. **monthlyCents: 0 means no money, not "no ceiling"** — the published fake and docs/match-api.md already said so (`projected > 0 &&`), only the T4 bootstrap comment claimed otherwise; the comment was wrong and is fixed, the rule is now written in docs/operations.md, and the two orchestrator tests that allocated on a *priced* stub with a zero ceiling got a real one (a key that may spend nothing can only use free providers, which is the wall doing its job and the right default for a dev key). fleet.budget_threshold: **the published BUDGET_THRESHOLD_FRACTIONS [0.8, 0.95] won over the task line's "50/80/100 %"** — the constant cites decision 7, is exported to clients, and is pinned by the fake, its tests and the conformance suite; changing a number a consumer already reads for a summary would have been contract churn for nothing (the references win over the task summaries). Announced into every open match of the key (the fleet webhook is T31's), once per (ceiling, fraction, month) — the mark is a **row**, api_key_budget_notices, the round's one additive migration here (0001_budget-notices), because "never repeated for the same crossing" cannot live in a process that restarts every deploy; moving a ceiling deletes that key's marks in the same transaction, a new number being a new crossing. The check runs when a ledger row opens (tracked, never awaited: the warning is emitted on the very chain the walk is holding) and on a sweep every minute over **only the keys with an open row** — a ratio climbs when a server runs and nowhere else. GET /v1/fleet/budget serves the calling key's limits + usage. Contract, additive, **@ezpug/match-api 0.2.0** with its changelog line: POST /v1/keys/:keyId/rotate (a new secret, the old one dead on the spot, same id/scopes/budget/webhook secrets, invalid_state for a revoked key) and PATCH /v1/keys/:keyId/budget (BudgetPatchRequest — any of the three, at least one, the rest untouched); both served by the fake and by the orchestrator, both documented in docs/match-api.md, the doors T2 left open. Store work: listKeyLedgerSince on the match store (open rows, plus rows closed inside the month) and rotateSecret/setBudget/markBudgetNotice/clearBudgetNotices on the key store, all four across memory and Postgres with the contract tests extended. **P1 found and fixed, not retried into green:** the extended conformance's stream-hello flow failed 1 run in ~10 with "the first frame is a hello — event". stream/upgrade.ts subscribed *before* reading the match for the hello (deliberately, so nothing published in the gap is missed) and sent whatever arrived in that window first; T5's extra fact at allocation made the window's occupant common enough to see. Frames that arrive before the greeting are now held and flushed right after it — both promises kept — with a new stream/upgrade.test.ts that holds the hello's read open and fails against the old code; eight consecutive conformance.extended runs green afterwards. Tests: 11 in src/budget (each ceiling, a free provider under a zero ceiling, accrual on a live row, a closed row, the month turning at 2026-10-01T00:00:00Z, the route, the warnings once per crossing and again after a ceiling moves, the sweep on the clock, rotation and the patch over HTTP) on a *priced* sim standing in for T15's fake Dathost at 60 cents an hour — the fake Dathost does not exist yet, so the price is what was borrowed, not the fake. pnpm verify and pnpm verify:extended green (orchestrator 151 passed / 6 skipped, conformance against the real service 9 passed / 3 skipped). — 1dd7d52 — 2026-09-05
T6: the link. apps/orchestrator/src/link/server-link.ts: /link is a raw `ws` upgrade on the router T3 put before Hono, attached before the port opens, and every socket listener is added before anything is awaited — a frame that lands while the token is being looked up queues on the session's chain, it never vanishes (the platform's Socket.IO lesson). hello first: the token hashed and looked up in server_tokens, the row must be open, everything else a LINK_CLOSE_CODES code that says why (4001 unknown/revoked/closed row, 4002 protocol, 4003 malformed or not-hello-first, 4005 replaced by a newer socket with the same token, 4008 no hello in ten seconds, 4012 shutting down; a draining orchestrator answers the upgrade itself with a 503). What hello reports lands on the row (versions, hostname, current_map, link_state — T2's columns, **no migration**) and the token's last_used_at is written; welcome carries provider + serverId and ackedSeq; then assign unless hello says the server already holds the row's match. **The assignment is composed once, pure, in link/assign.ts** (manifest minus maps/widget, plugins = the manifest's + WeaponPaints when a roster loadout needs it and the image has it, cvars merged request-under-mode-under-rules so a request never undoes what a mode needs, roster with every pushed profile applied, warmup lines, branding, demoUploadUrl; matchzyConfig is T9's, restore T14's) and parsed through the frame schema so the bytes are the schema's order; a manifest naming a plugin the hello did not list fails the match provider_error before anything is sent. **Ack per event, contiguous ackedSeq**: at-or-below it or already in the window above it is duplicate, the rest go to the machine in order and its answer is the ack (accepted/ephemeral/rejected with a reason for an event naming a match the server does not hold); ackedSeq persisted on the row after every batch so welcome.ackedSeq survives a restart on either side; a hello whose lastSeq is below ours is a fresh process and ours follows it. The window above the contiguous mark is not persisted: an out-of-order event accepted in a gap is resent by the peer after a reconnect and ingested again — at-least-once, and the vocabulary's per-match `seq` is the exact dedup the machine already applies. **Answers are resolved off the inbound chain**: the machine sends a command while holding the match's chain and a real plugin reports the pause *before* it answers the command that caused it, so command_result / console / player_command_result are matched by correlationId the moment they arrive, everything else runs in order; the deadline is 15 s on the clock → provider_unavailable. Heartbeats re-arm the machine's loss detector and a silence timer at two intervals; silence → matches.suspect (the provider probed: running → the socket terminated so the plugin reconnects, gone → recovering with the newest backup's round → server_lost when nothing resumes it) — the machine gained `touch` and `assignment`, and `release` now goes down the link before the provider stops a server so the plugin unloads its mode and says idle while it still can. backup frames upsert (same match/map/round replaces, newest 8 per match kept — MatchStore gains upsertBackup/listBackups/findServerTokenByHash/touchServerToken across memory and Postgres, contract test extended); console tails cached on the session for T20. **The fake server** (`@ezpug/protocol/fake-server`): no timers, no clock, no backoff — a test drives it; it sequences from 1, buffers until an ack names a seq, resends past welcome.ackedSeq in batches on reconnect, answers commands by correlationId, reports state after assign/release/drain, records every frame each way; five exchanges recorded over a real socket in server-link.test.ts (match, refusals, events, reconnect, commands), scrubbed to FIXTURE_MATCH_ID and a token that says not-a-secret, checked canonical + secret-grepped by the protocol package and round-tripped byte for byte by ProtocolRoundTripTests.cs (39 xunit now). Recovered from a run that died mid-task: the tree already held the whole thing; reviewed, verified, nothing thrown away. docs/operations.md "The link", packages/protocol README. Checked by hand: the real composition on this box answers /link with 4003/4002/4001. pnpm verify green (orchestrator 158 passed / 6 skipped, protocol 33, C# 39), pnpm verify:extended green. — f5dc44a — 2026-09-05
T7: EZPug.Sdk. plugins/EZPug.Sdk (0.1.0) + a second project EZPug.Sdk.Testing (the fakes and the harness, so a server's plugin folder never carries them; added to EZPug.sln). The seams: IGameWorld (players with SteamID64/slot/team/alive/position/health/armor, say/print/center/HUD/console, give/strip/respawn/health/armor/speed/team/kick, exec cfg + command, cvars, changelevel + host_workshop_map, and every engine hook as a C# event: map, connect/disconnect/spawn/death, round start/end, bomb, chat, tick), IPlatformLink (emit, state, backup, console, AnswerCommand; inbound through IPlatformLinkHandler, delivered by Pump() on the caller's thread), IClock (monotonic NowMs = the uptime on the wire, After/Every; SystemClock on Stopwatch + thread-pool timers for the link; a mode's clock is the world's, which T8 builds on game-thread timers). Gamemode base: Id, CreateLocalizer, OnAssigned/OnStart/OnPlayerJoined/Left/Spawned/Died/OnRoundStart/End/OnChat/OnPlayerCommand/OnProfile/OnCommand/OnTick/OnDrain/OnEnd, Emit/EmitPluginEvent, Lines/Say/SayAll/PrintCenter localized per player, PlayerState<T> (dropped on leave, cleared at release), After/Every (cancelled at release). GamemodeRuntime: the link's handler and the world's listener — assign → host's Assigned (the loader) → OnAssigned → state assigned "plugins loaded"; a mode attached after the assignment (hot-loaded by the loader) hears OnAssigned at once; **a mode whose Id is not the assignment's hears nothing** (the harness found the runtime routing hooks to it); release → OnEnd → timers/state cleared → host's Released → state idle; announce/kick/rcon/profile answered by the runtime, the rest by CommandHook (host) or the mode, default command_unsupported; **the runtime emits the plumbing/gameplay events once from the world's hooks** (server_ready, player_connected/disconnected for humans, player_death, bomb_*, chat_message/chat_command by the vocabulary's prefix rule ported as ChatParser) so T8 stays thin; match-flow events are the flow owner's (MatchZy/T9, the generic emitter/T22, or the mode). Facts = the event model: every vocabulary event with matchId/source/mapNumber/roundNumber filled from MatchContext; Player() maps engine team → team_a/team_b/spec by roster first, sides in effect second, team_a for a one-team mode; emitting going_live/side_swap/map_end/series_end through the runtime advances the context; every emitted event (not position ticks) gets the per-match seq via the record's own <Clone>$ (EventStamper). CommandTable: the manifest's player commands enforced and never trusted to the phone, in the refusal set's order — unknown_command, invalid_args (ArgsValidator: the JSON Schema subset type/properties/required/additionalProperties/enum/const/min/max/minLength/maxLength/items/minItems/maxItems), cooldown as a deadline on the clock (message rounds up), no_charges per life (spawn) / round / map / match — then the mode; only an applied outcome spends; !verb in chat runs the same pipeline and answers in chat. Localizer: DE default, mode catalog over the SDK's, [key] for a miss; **resx parsed by the SDK itself, embedded as raw XML (Type="Non-Resx")** rather than resgen + satellite assemblies, because a hot-loaded plugin's culture subfolder is one more thing that can silently go missing; a test insists the SDK's de/en keys are equal. LinkClient: hello first (token, versions, capabilities, plugins, hostname, map, state, matchId, lastSeq from the buffer), welcome → drop ≤ ackedSeq, flush the rest in EVENTS_BATCH_MAX batches, heartbeat timer on the clock; acks remove from the buffer; command/console/player_command answered with their correlationId from the pump; **CommandAnswer.Deferred + AnswerCommand for work that waits on the engine** (commands.json leaves cmd-0004 unanswered on purpose, which is what surfaced the need); reconnect with capped exponential backoff on the clock (1 s → 30 s, reset by welcome) after a lost socket, a refused dial, 1000/1001/1005/1006/1011/replaced/shuttingDown; unauthorized/protocolMismatch/malformed/revoked stop the loop and reach the handler as fatal; the newest backup is kept until a welcomed socket takes it; unreadable frames are logged and the link stays up; nothing logs a token or a frame. IEventBuffer: MemoryEventBuffer and FileEventBuffer (events.jsonl with a {"lastSeq":n} header always present — a fresh file's first event was mistaken for the header until it was — plus acked.log, compaction past 500 acks, torn lines skipped and counted, flushed to the OS not fsynced because a position tick every 100 ms cannot afford one). Sidecar: EZPUG_IRON_URL + EZPUG_SERVER_TOKEN (+ EZPUG_LINK_BUFFER_DIR) win over ezpug.json {url, token, bufferDir?}; a base URL becomes the link URL (https → wss + /link); ToString redacts. **The recorded link exchanges are the arbiter:** LinkClientFixtureTests replays all five packages/protocol/fixtures/link/*.json through the real client over a scripted socket — the orchestrator's frames in, every server frame the fake sent must come out byte for byte in order (hello, the state answers, the flush after welcome, the answers by correlationId; events/backups/heartbeats/unsolicited consoles are driven through the public verbs, offline emits are inferred from hello.lastSeq, the fake's two deliberate misbehaviours in events.json go through the raw send and the test says so). Harness: FakeClock, FakeGameWorld (records every verb, scripts every hook), FakePlatformLink (records events/states/answers, pushes assign/release/command/player_command/profile synchronously), GamemodeTestHost (AssignmentFor a manifest the way link/assign.ts composes one, ManifestFrom the shipped JSON), ScriptedLinkSocket(+Factory). The fifty-line mode from docs/sdk.md (EZPug.Sdk.Tests/Modes/PowerupDemo with its de/en resx) plays a match on the harness from the shipped powerup-dm manifest: tap lands (speed 1.4, "Power-up aktiv: Tempo.", plugin_event), no_charges, not_alive in English, invalid_args, unknown_command, not_in_match, !powerup in chat, chat_message, a death and the respawn timer on the clock refilling the life, announce/kick/pause/unknown-kick over the link, release with timers and state gone, the exact event list with seqs 1..n. docs/sdk.md written (the shape, the fifty lines, hooks, event model and who emits what, player commands, the link, testing a mode, what T8 adds). No CounterStrikeSharp adapter yet — that is T8's shell; no dev CS2 container exists before T10, so no real-server check was possible and none is claimed. pnpm verify green (C# 104 xunit, orchestrator 158 passed / 6 skipped, protocol 33); verify:extended not demanded for T7 (the extended conformance ran anyway under verify because the dev world was up: 9 passed / 3 skipped). — 59fc70c — 2026-09-05
T8: EZPug.Core, the plugin every server runs. plugins/EZPug.Core (0.1.0) + EZPug.Core.Tests, both in EZPug.sln. Exactly two files touch CounterStrikeSharp: CounterStrikeWorld (IGameWorld over the engine — EventPlayerConnectFull for humans and OnClientPutInServer for bots, EventPlayerDisconnect, spawn, death with assist/weapon/headshot/penetrated/noscope/smoke/blind, round start/end with the reason folded to the SDK's four and the score from cs_team_manager, bomb 0/1 → A/B, say/say_team command listeners → ChatSaid, OnTick → the clock + Tick; verbs as controller calls or console lines, cvars set as a console line so a plugin's fake cvar and an engine cvar take it the same way, GetCvar by ConVarType, kick via kickid, everything sanitized of quotes/semicolons/newlines) and CorePlugin (Load builds the paths from ModuleDirectory, the GameThreadClock, the world, the PluginCatalog, the sidecar, the LinkClient on SystemClock with a FileEventBuffer, the runtime, the loader, the host capability, and runs RunAsync on the pool; Unload withdraws, cancels, closes the link bounded to two seconds, disposes). **The map is announced to the SDK one second after OnMapStart** (MatchZy waits the same second) because the engine execs its own gamemode cfgs right after that listener and a cfg exec'd before them is undone. **Layout decided:** the core at plugins/EZPug.Core, every manifest-nameable plugin under plugins/disabled/<Name>/ (CounterStrikeSharp skips that folder at boot; the loader hot-loads it), EZPug.Sdk.dll once under shared/EZPug.Sdk/ and never beside a plugin — the host capability is keyed by an SDK type and two copies would be two types. **css_plugins is spoken in one form only, the dll path relative to addons/counterstrikesharp** (plugins/disabled/MatchZy/MatchZy.dll): CounterStrikeSharp composes a bare name differently for load (plugins/{path}/{basename}.dll) and unload (plugins/{id}/{id}.dll), and only the .dll form lands on the same file both ways. The loader (pure over IGameWorld + PluginCatalog, proven on the harness): Assigned → hostname (branding or EZPug · <mode> · <Map>), css_plugins load per named plugin (a missing one warned and skipped, never fatal — the orchestrator refuses before sending), changelevel or host_workshop_map for an all-digit id; **MapLoaded — a new host hook on the runtime, fired after the map is up and before server_ready/OnStart** → exec the cfg in order, the flat cvars, and for flow matchzy write cfg/ezpug/match.json (matchzy_loadmatch is relative to game/csgo) + matchzy_loadmatch; Released → unload in reverse, the config removed, changelevel to the lobby (EZPUG_LOBBY_MAP or the boot map), then the runtime says idle. assign.restore is warned about and left to T14. hello: versions plugin/sdk from the assemblies, counterStrikeSharp from Api.GetVersionString() (the SDK's pin as fallback), matchzy read off its dll's manifest without loading it, metamod null; **capabilities reported honestly: positions, chat, playerCommands, widget** — backups (T9/T14) and scoreboardRating (T27) are not claimed before they exist (the orchestrator does not match on them today). Sidecar: EZPUG_IRON_URL + EZPUG_SERVER_TOKEN, else game/csgo/ezpug.json (the T16 upload target); neither → UnlinkedPlatformLink (the plugin loads, events are dropped, said once, ezpug_status says so). Console commands, server-only: ezpug_status (versions, link host and provider/serverId — never the token or the URL's query, buffer lastSeq/unacked, state/map/players, match/mode/round, plugins enabled/installed), ezpug_announce <text>, ezpug_restore <file> <round> (a name, not a path; must exist under game/csgo; mp_backup_restore_load_file — the recovery flow around it is T14). Console tail = what the plugin logs + announces + restores; CounterStrikeSharp has no hook on the engine's own console output, said in the README. **SDK growth:** GamemodeRuntime.MapLoaded; a position ticker (PositionTickIntervalMs 100, only while assigned + capability + Link.Connected + somebody alive, unsequenced, never buffered for a dead link) with FakePlatformLink.Ticks kept apart from Events so every exact story assertion stays exact; BotIdentity (bots have SteamID 0 and the vocabulary insists on 17 digits: 90000000000000000 + slot, IsBot reads it back) so a bots-only match (T13) emits valid deaths; EZPug.Sdk.Hosting — IGamemodeHost + GamemodeHost (a PluginCapability; Publish/Withdraw keep one supplier per process answering with the current host, because RegisterPluginCapability appends and a hot-reloaded core would otherwise leave a stale host first in line; Find() is null-safe where Get() throws KeyNotFound) + GamemodePlugin, the five-line CounterStrikeSharp shell of a mode (attach on Load, retry OnAllPluginsLoaded, detach on Unload only if it is the attached one). plugins/publish.sh (pnpm plugins:publish) → plugins/dist with exactly EZPug.Core.{dll,deps.json,pdb} + build.json and shared/EZPug.Sdk/EZPug.Sdk.dll: dotnet publish also emits CounterStrikeSharp.API and forty Microsoft.Extensions.* dlls the runtime already ships, dropped on purpose (a copy beside a plugin shadows the host's); T10's image and T18's template take this tree. plugins/README.md: the layout drawn, install by hand in five steps, the env/ezpug.json contract, the commands, threads in a paragraph, a gamemode plugin in five lines, what is not here yet; docs/sdk.md: the host hooks table, ticks, bots, the hosting section. **No real server exists before T10, so CounterStrikeWorld and CorePlugin are proven by compiling at the pinned 1.0.373 and by the harness tests of everything behind them; nothing is claimed run on CS2.** Tests: EZPug.Core.Tests 22 (catalog on a temp image with real assemblies as dlls, server paths, the clock's due order/re-arm/cancel/no re-entry, the pug assignment line by line with the MapLoaded-before-server_ready proof and the release, reverse unload + a missing plugin, the config mode, a workshop id, branding, the T14/T9 warnings, the status line with no secret, the hello, the restore door's refusals, the unlinked link), EZPug.Sdk.Tests 110 (+6: MapLoaded order, ticks for humans and bots, no ticks without the capability, BotIdentity, the capability across a re-publish). pnpm verify green (C# 132, orchestrator 158 passed / 6 skipped, lint + boundaries); verify:extended not demanded for T8 (the extended conformance ran under verify anyway because the dev world was up: 9 passed / 3 skipped); the EZPUG_CS2_TESTS lane does not exist yet. — 12ce1cb — 2026-09-06
T9: pug — MatchZy, translated once, and the config builders. **Decided by reading MatchZy 0.8.15 (`/root/ezpug/references/MatchZy`, ef289d5): there are no in-process forwards** — `events_and_forwards.md` is the HTTP remote log and nothing else (`PublishEvents.cs`: one POST per event, one custom header, a 15 s timeout, no retry, no dedup), so decision 19's fallback branch is the whole path and nothing is subscribed in the plugin; T39 amends the decision's wording. The orchestrator side: `apps/orchestrator/src/match-config/matchzy.ts` — the platform's `buildMatchZyConfig`/`buildGet5Config` ported with the Match API request as the input (the platform's `match-config.ts` only survives in a worktree there; its copy is gone from main), goldens `fixtures/{matchzy-pug-bo1,matchzy-knife-bo3,get5-bo1}.json` plus MatchZy's `ValidateMatchJsonStructure` transcribed; `matchid` is `matchzySerial(matchId)` (SHA-256 folded to a positive 31-bit int — MatchZy `int.TryParse`s it, a uuid is not one) and the door checks every event carries it; `players_per_team` is the manifest's `teamSize` (an unrostered bot match still plays five a side); the config's `cvars` are the assignment's flat map (`match-config/cvars.ts` holds the one precedence rule both use, breaking an import cycle) because MatchZy's `live.cfg` resets the round format and MatchZy re-applies its config's cvars a second later; **no secret and no hostname in the file** — the platform's builder wrote the remote-log cvars with a token, ours does not, because MatchZy serialises its whole config (`GetMatchConfig()`, `RemoteLogHeaderValue` included) into every round backup. `composeAssign` carries it as `matchzyConfig` for a `matchzy` flow; the five link goldens were re-recorded (the scrub now also rewrites `"matchid":<serial>` so the fixture's serial is the fixture id's) and the C# replay is still byte for byte. **The door** `POST /matchzy/log` (`MATCHZY_LOG_PATH`, `MATCHZY_TOKEN_HEADER`, `MATCHZY_PAYLOAD_MAX` in `@ezpug/protocol` → `ProtocolConstants`): the server's own link token in `x-ezpug-server-token` (a header, never the path — Traefik's access log would hold a path), hashed → `server_tokens` → the open ledger row → its match; the payload translated (`matchzy/translate.ts`, pure, state in/out) and ingested through the same `ServerEventSink` as the link, attributed to the same provider/serverId; 200 for everything readable (dropped events say why), 401/409/400/413/429 otherwise; mounted in `app.ts` outside `/v1/` for both composition roots. **Translation rules, against the code not the schema:** `winner.team` in `round_end` is the map *leader* (`t1score > t2score`), so the round winner is the score delta since the last round seen, then the plan's side schedule (halves + overtime, `scheduledSide`, said in the log), then MatchZy's field on a knifed map; `winner.side` is `@event.Winner.ToString()` = `"3"`/`"2"`, read beside `ct`/`t`; `round_number` is the post-round score sum, so the vocabulary's 1-based round is the sum of the two scores; a tied `map_result`/`series_end` names team2 in MatchZy and is `winner: null` here; map names come from the request's plan; `series_start`, the veto trio, `demo_upload_ended` (T21 owns the upload) and `player_disconnect` (the plugin's own) are dropped; fourteen fixtures under `matchzy/fixtures/` marked `"source": "schema"`, and `fixtures.test.ts` reads the PRD's own T13 checkbox and fails the moment T13 is ticked with a schema-sourced file still present. `door.test.ts` plays a pug through both doors (server_ready over the sink, the rest over HTTP) into the events route. **The plugin side** (`plugins/EZPug.Core/MatchZy/`): the loader writes the config plus `matchzy_hostname_format` (MatchZy rewrites `hostname` from that cvar every round), `matchzy_loadmatch`s once per assignment (a later map is the series' next — the runtime bumps `MapNumber` for a matchzy flow on a second map), then sets the three remote-log cvars as console commands after the load (loading replaces MatchZy's config object; the file is in every backup) from `MatchZyRemoteLog.From(sidecar)` (`Sidecar.HttpUrl`); unlinked → a warn. `MatchZyFlow` observes what MatchZy cannot say: `match_paused`/`match_unpaused` off a new seam `IGameWorld.Rules` (a `GameRules` snapshot of `cs_gamerules`: warmup, rounds played, `m_bMatchWaitingForResume`, both tactical timeouts, the technical one, `m_bSwitchingTeamsAtRoundReset`; `FakeGameWorld.Rules` settable; `CounterStrikeWorld` reads the entity) polled every 250 ms — a timeout names its team via `Match.TeamOf`, the `pause`/`unpause` commands are MatchZy's `css_forcepause`/`css_forceunpause` and reported as an admin pause; `side_swap` at a round start outside warmup when the rostered team A stands on the other side (a knife winner's `.switch` and halftime alike) or, with nobody rostered, when the engine flagged the swap; 1.5 s after each live round start the newest `MatchZyDataBackup/matchzy_<serial>_<map>_round<NN>.json` as a `backup` frame (restores to round NN+1) plus `backup_written`, **scrubbed by `MatchZyBackups.Scrub`** of `RemoteLogHeaderValue` and any `*remote_log_header_value` cvar so the orchestrator's `backups` table never holds a token (T14 re-points the log after a restore). The runtime numbers rounds from `Rules.RoundsPlayed + 1` when the world has one (warmup and the knife round never count) and by itself otherwise. `hello` now claims `backups`. `gamemodes/pug/cfg/ezpug/pug.cfg` written (no hibernation, no votes, GOTV on, `bot_quota 0` unless a request's `rules.cvars` says otherwise — T13's `bot_quota 10` survives `live.cfg` only because the config's cvars are re-applied after it). **Two P1s found while verifying, both pre-existing, both fixed:** (1) `conformance.extended` `stream-hello` failed 2 of 3 isolated runs and 1 of 2 on the pre-task tree — `stream/upgrade.ts` held frames published while the hello was being read (the T5 fix) but flushed them unfiltered, and the machine appends to the log before it publishes, so a frame with the hello's own `seq` reached the client and replaying from the cursor showed it twice; held events at or below the hello's seq are now dropped, `upgrade.test.ts` holds the read open and proves it, five extended runs green after; (2) a `pnpm verify` that failed at typecheck made Turbo kill the in-flight orchestrator test run, the extended conformance never reached `afterAll`, its twenty keys and five sim rows stayed in the shared test database, the next run collided on the per-file stable key names and `store.contract.test.ts` found a leftover `sim-1` row newer than its own — the extended suite now sweeps its whole namespace before and after a run (`sweepNamespace`, by name, not by what this run minted) and the contract test uses a handle of its own and reads every unfiltered ledger listing through its key. Docs: `docs/gamemodes.md` "The `matchzy` flow", `docs/operations.md` "The MatchZy door" + the hello rule, `docs/sdk.md` (rules, rounds and maps, the hooks table), `plugins/README.md`, the protocol README. No real server exists before T10, so nothing here is claimed run on CS2 or against a real MatchZy: the translation is pinned to the source and the schema until T13 records the real thing; the `EZPUG_CS2_TESTS` lane does not exist yet. pnpm verify green (orchestrator 189 passed / 6 skipped incl. 30 new, protocol 33, match-api 305, C# 32 core + 113 SDK), pnpm verify:extended green (the conformance against the real service 9 passed / 3 skipped, ~56 s). — 993b715 — 2026-09-06
T10: the server image. docker/cs2/Dockerfile in three stages — `plugins` (the .NET SDK tag exactly plugins/global.json's, restore layered off the csprojs, `./publish.sh /out`, EZPUG_GIT_SHA in as a build arg because .git is not in the context and publish.sh now prefers it over `git`), `vendor` (Metamod 2.0.0-git1411, CounterStrikeSharp 1.0.373 *with runtime* — the steamrt base ships no .NET — and MatchZy 0.8.15, each a URL + an `ARG *_SHA256` verified with `sha256sum -c`; MatchZy lands in plugins/disabled/MatchZy with its cfg/MatchZy set, retakes and WeaponPaints are a documented empty slot for T23/T28), and `cs2` (steamrt sniper pinned **by digest**, non-root `steam` uid 1000, EXPOSE 27415/udp+tcp and 27420/udp, HEALTHCHECK on `pgrep -x cs2`). **All three checksums were re-fetched and verified byte for byte against the live URLs, not trusted.** check-pins grows four cases reading the ARG lines and the FROM digest, plus the one cross-check that matters: the CounterStrikeSharp the plugins *compile* against (Directory.Build.props) must equal the release the image *ships*, because a plugin loaded by the wrong API is invisible until a server boots with half its plugins missing. The game is not in the image: install-game.sh is its own entrypoint (steamcmd, app 730, `--validate`) and the boot entrypoint **refuses to start** rather than download 67 GB on a Saturday. entrypoint.sh every boot: the image's addons over the volume's, the Metamod line back into gameinfo.gi (anchored after Game_LowViolence, tabs and all — a game update rewrites that file), each `gamemodes/<id>/cfg/` by name into game/csgo/cfg/, Steam's client libs into ~/.steam/sdk64, then `exec`s cs2 so it is PID 1 and `docker attach` is a real console. compose.cs2.yaml (host network — a game server's clients read the address out of the UDP packets), scripts/cs2-env.sh + `pnpm cs2:build|install|up|down|status|logs|console`. **Proven on real hardware, which nothing before T10 could be:** image built (reproducible — a second build was fully cached to the same id), game installed, and a CS2 server booted with Metamod taking libserver.so, CounterStrikeSharp starting its .NET 10 runtime, and `Loading plugin EZPug.Core` → `EZPug.Core 0.1.0 on EZPug.Sdk 0.1.0; … installed plugins: EZPug.Core, MatchZy; unlinked` — the first time this repo's plugin has ever run on CS2 — then de_dust2 loaded, warmup ticking with bots, container `healthy`, **0 restarts**. **Three real defects found by that boot and fixed, none of which any test could have caught:** (1) `steamclient.so` is absent unless steamcmd has run once, and a dedicated server without it exits **0** seconds after its plugins load — a clean shutdown to every eye, and under `restart: unless-stopped` a silent boot loop (observed: 6 restarts); the image now does that first steamcmd run at build time, as the `steam` user, because as root steamcmd dies "Failed to load steamconsole.so". (2) The **RCON password leaked into `docker logs` in clear**: the engine strips `+rcon_password` from the line it prints, but CounterStrikeSharp echoes the *raw* command line at boot, so the entrypoint's care was defeated; it is now written to a `0600 cfg/ezpug/rcon.cfg` and `+exec`'d, which also keeps it out of /proc/*/cmdline, and the test greps the entrypoint's code (not its comments) for `+rcon_password`. (3) The documented install size was a guess — **the truth is ~67 GB installed from a ~71 GB download**, measured here; every place that repeated "35–40 GB" (Dockerfile, entrypoint, install script, cs2-env.sh, compose, .env.example, README, operations, pins, the test) now says the measured number. Also fixed while it was happening: two steamcmd processes were writing the volume at once — the previous run's install container outlived the shell that started it — so `cs2:install` now refuses when one is already running and says how to follow or stop it, and the volume was re-installed with `--validate` after the overlap. Not claimed: `ezpug_status` was **not** run on the real server (the console needs a TTY and a piped `docker attach` refuses), and nothing was linked — no server token exists until a provider allocates one, so `hello`/`assign`/the loader remain harness-only until T12/T13. Docs: operations.md "The CS2 server image" incl. the two boot lessons, pins.md rows for Metamod/CSS-release/sniper-digest/steamcmd with the checksum-refresh recipe, README quickstart, plugins/README corrected (it claimed the plugin had never run on CS2). **P1 found, filed, not retried into green:** the conformance suite races the sim's story under load — `happy-bo1`'s pause/announce refused `invalid_state` and webhook deliveries went missing in 2 of 3 and 2 of 4 loaded full-suite runs (0 of 10 for the file alone, green on an idle box), traced to the runner's 10 s fake-clock poll dealing many beats whose ingests the sim channel deliberately does not await, so `createTestApp`'s `settle()` returns mid-story; tracking the ingest in the machine's `inflight` set was tried, **did not fix it**, and was reverted rather than shipped inside T10 — written up as **T10a** with the reproduction. pnpm verify green (28 orchestrator files / 198 passed / 6 skipped, match-api 29, sim 7, protocol 4, core 4, Biome 244 files, C#); verify:extended not demanded for T10 (the extended conformance ran under verify anyway because the dev world was up). The dev CS2 container was stopped afterwards; the 67 GB volume is kept for T12/T13.
T10a: the conformance suite races the sim's story under load. **T10's diagnosis was one file off, and that is why its fix did not take:** the failures are `conformance.extended.test.ts`'s, not the in-process `conformance.test.ts`'s — the extended file only runs when the dev world is up, so it rides along in a full `pnpm vitest run` and vanishes when the flake is chased in the fake-clock file alone (T10's "0 of 10 for conformance.test.ts" was measuring a file that was never failing). Reproduced **3 of 5** loaded runs of that file by itself (sixteen busy CPU loops beside it), with both of T10's symptoms in the same runs: `a pause is accepted — invalid_state`, `an unpause is accepted — invalid_state`, `the commands landed as events`, and `every durable envelope was delivered by webhook — never delivered: 68,69`. **The mechanism has nothing to do with the fake clock:** that tier has none. The story plays on real timers at sixty times real time while the client talks over a real socket, and the target's `settle()` was `sleep(500)` and its `advance()` `sleep(250)` — hopes, not barriers. Two independent consequences, one per symptom. (1) The half second returned while the last envelopes were still queued, so `playToEnd`'s replay outran the webhook worker — the delivery check. (2) `SHORT_RULES` is two rounds ≈ four match-minutes, so at 60× *everything* between `going_live` and the last event fitted in about four seconds; on a loaded box a client's own round trips eat that, the flow's `pause` reaches a match the machine has already ended, and `machine.ts` refuses it `pause only while live`. Neither is fixable by tracking a promise, which is why T10's `inflight` attempt moved the failure and did not remove it. **The fix, both halves in the target:** `quiesce()` asks the orchestrator — which runs in the same process — when it has caught up (`matches.settle()`, `webhooks.settle()`, `listDueDeliveries` empty, bounded by passes and by `QUIESCE_TIMEOUT_MS`; a row in backoff is *not* due, so a flow that means a delivery to fail still gets its failure), and `SIM_TIME_SCALE` drops 60 → 20 so the live window is a dozen seconds instead of four. The per-flow `it()`s needed a budget to match: `describeMatchApiConformance` grew an optional `timeoutMs` (`fixtures/vitest` is deliberately **not** in `publishConfig.exports`, so this is no contract change and no release), the gate's own budget went 120 s → 420 s. **The same hole in the fake-clock barrier, closed while it was open:** the sim provider dropped the `ingest` promise it started from a story timer's callback (`provider.ts`) and from a channel's `say` (a command reports what it did while the machine still holds that match's chain) — nobody's to await, and worse, *invisible*, so a barrier could return on a match that was still moving. Both now go through one `trackedSink` that registers the promise before it hands it back, `SimProvider.pending()/settle()` expose it, and `createTestApp`'s `settle()` may only return when `sim.pending()` is zero as well as when no zero-delay timer is armed — its doc comment now names all three things that can still be moving. A new sim-provider test holds the invariant with a sink that answers only when the test says so: after a beat, `pending() > 0`; `settle()` does not cross while the ingest is unanswered; it crosses when it is. **Proof, never retried into green:** the loaded reproduction (the whole orchestrator suite, sixteen busy loops) run **ten times, ten green**, each one with the extended suite actually running (199 passed / 6 skipped, no "skipping the extended conformance" line in any of the ten). The cost is honest and is the trade: the orchestrator suite goes from ~61 s to ~150 s, because the extended stories now play at a third of the speed — margin bought with wall clock, in the one tier whose job is to be believed. docs/operations.md gains "Waiting is a barrier, never a sleep". pnpm verify green (28 files, 199 passed / 6 skipped, C# 32 core + 113 SDK), pnpm verify:extended green (16/16 then 3/3). — acc3267 — 2026-09-06
T11: ezpug-node. apps/node (`@ezpug/node`, tag app, `pnpm node <verb>` from the checkout, `pnpm node:build` for the image): one process, five verbs — `enrol <token>` (dials `/node` once with the one-time token, keeps the node token `welcome` hands over in `node.json`, 0600 in a 0700 dir written by rename, and hangs up; also checks docker and pulls the server image so a broken box fails at enrolment and not on the first match), `run` (adopts the containers a previous agent left from their labels, pulls the image when absent, hello, keeps dialling), `status` (identity, docker, image digest, containers, the link's last heartbeat — never the token), `forget` (the node's half of a revoke), `health` (the image's HEALTHCHECK: exit 0 iff a heartbeat went out within three intervals, read from `health.json`). **The link client** (`link.ts`): hello first with `tokenKind` enrolment|node, nothing before welcome, heartbeats on the welcome's interval on the clock, start/stop/drain/undrain applied in arrival order on one chain, backoff 1 s doubling to 30 s counted per *failure* and reset by a welcome, **4001/4002/4003/4009 stop the loop for good** (`run` exits 2 saying `forget` + a fresh token) while 1006/4005/4012/a 503 refusal redial. **The containers** (`instances.ts`): a `start` spec becomes `ezpug-node-<id>` from `spec.image` on the host network with the game volume at `/serverdata/serverfiles` (+ a checkout's `gamemodes/` read-only when `EZPUG_NODE_GAMEMODES_DIR` says), the spec's env plus `EZPUG_SERVER_TOKEN`/`EZPUG_IRON_CS2_GAME_PORT`/`EZPUG_IRON_CS2_GOTV_PORT` (what `docker/cs2/entrypoint.sh` reads), a tty, no restart policy (the node is the supervisor), and eight `com.ezpug.node.*` labels (managed, node id, instance, purpose, server id, match id, both ports) that are how a restarted agent adopts them — a node restart never ends a match, the containers keep running and the next hello carries them. **The protocol has no "no" frame, so a refused start is a `failed` instance with the reason** (capacity, a port another instance holds, drained, unenrolled, an image that will not pull, docker's own error), kept in the snapshot until the orchestrator's idempotent `stop` clears it; every change is a whole `instances` snapshot, never a delta. Docker is *polled* on the clock (`EZPUG_NODE_POLL_INTERVAL_MS`, 2 s) rather than streamed so a fake clock makes an exit (`failed`, exit code) or a vanish (`failed`, said so) instant and deterministic. **The warm pool is the orchestrator's to fill:** the node advertises `capacity.warm` (`EZPUG_NODE_WARM`, refused above `EZPUG_NODE_MAX_INSTANCES`) and runs the `purpose: warm` starts T12 sends — a warm instance is a server with a server token, and only the orchestrator mints those (T1's protocol already said so; written down in docs/nodes.md and the code). **The docker port** (`docker/port.ts`): dockerode **4.0.12** (+ `@types/dockerode` 4.0.1, catalog + pins row + check-pins case) — 5.x drags a BuildKit client with gRPC and protobufjs for nothing the agent does; the adapter is idempotent where docker is not (304/404 on stop and remove are the outcome asked for), the digest is the registry's `RepoDigest` or the image id for a never-pushed local build (which is exactly what `cs2:dev` on this box reports), and it is **proven against the real daemon** in `dockerode.test.ts` when the socket exists (busybox, a per-run label, nothing left behind; skipped with a printed reason otherwise, `EZPUG_NODE_DOCKER_TESTS=required` makes that red); `docker/fake.ts` scripts exits, vanishes and per-verb failures for every other test. Tests (61, ten files): config, state file modes/rename/refusal of a foreign file, log redaction of every `ezi<kind>_` token, the instance manager over the fake docker (exact container spec, starting→running snapshots, pull and pull failure, capacity/port/drained/unenrolled refusals, docker create/start failures and the retry that removes the old container, resent start ignored, stop idempotent and a stop docker refuses, exit and vanish on the clock, adoption from labels with a stray container warned about once, start-then-stop ordering), the link over the real WHATWG socket against a scripted `/node` endpoint (`testing.ts`: a `ws` server that welcomes, refuses with any code, stays silent or terminates) — the exact backoff sequence 1/2/4/8/8 s with the reset, every fatal code, the hello deadline, the hello recomposed per dial, 1000 from our side with no timer armed — the agent end to end (enrol writes the identity and no line holds either token, refuses a second enrolment and another orchestrator's identity, start → container with the token in its env → two snapshots, heartbeat → health.json, stop → gone, drain via welcome and via frame, adoption after `close()`, a death on the clock, reconnect after 4012 with the instances in the second hello, 4009 ends `run()`), the CLI's every verb with exit codes, and `docs.test.ts` holding docs/nodes.md, .env.example and the Dockerfile to the config's variables, the CLI's verbs and the orchestrator image's Node. `docker/node/Dockerfile` (the orchestrator's recipe: same base, `pnpm deploy --legacy --prod`, one bundle; non-root with `--group-add "$(stat -c %g /var/run/docker.sock)"` for the socket rather than root for one fd; `EZPUG_NODE_STATE_DIR=/var/lib/ezpug-node` on a VOLUME; ENTRYPOINT the bundle, CMD `run`, HEALTHCHECK `health`). **Checked by hand on this box:** `pnpm node status` and the built image (`ghcr.io/ezpug/ezpug-iron/node:dev`, 248 MB) both read the real daemon through the socket as uid 1000 and report `cs2:dev`'s digest; `health` exits 1 before any link; `enrol` against the dev orchestrator fails honestly with `1006 (non-101 status)` because `/node` does not exist there until T12 — **no node has been welcomed by a real orchestrator yet, and no real CS2 container has been started by the agent; both are T12's** (the container spec is pinned to what `compose.cs2.yaml`/the entrypoint read, by test). docs/nodes.md (five commands on a venue box, what the agent does, configuration, when something is wrong, what is proven), .env.example `EZPUG_NODE_*` block, `.ezpug-node/` gitignored, operations.md and README pointers. pnpm verify green (node 61, orchestrator 199 passed / 6 skipped with the extended conformance running under it because the dev world was up, C# 32 + 113, 20 pins); verify:extended not demanded for T11 (no flow touched). — 8c6db00 — 2026-09-06
T12: the node provider and enrolment. `/node` on the orchestrator (`link/node-link.ts`) is the server link's smaller twin — same raw `ws` upgrade on the router in front of Hono, same "every listener before anything is awaited" rule, same `hello`-first handshake and close codes — but it carries *containers*, never a match, and nothing is acked (an `instances` frame is a whole snapshot, so a lost one is superseded by the next and a reconnect's `hello` carries the state). **Enrolment is two steps and only the second makes a lasting secret:** `POST /v1/fleet/nodes` writes the row and mints a one-time `ezie_` token shown once (hashed into `node_enrolments`, 24 h); the node spends it on its first `hello`, the link mints the `ezin_` node token and hands it over in the `welcome` — the only time it is readable. `DELETE` revokes and closes the socket `4009`; re-`POST`ing a live id is a rebuild (fresh one-time token **and** the node token in force revoked, because two agents answering for one node would have the pool counting its capacity twice); `drain`/`undrain` write the row and tell the agent. What the `hello` says (region, labels, capacity, version, image digest) lands on the row, so a re-labelled node needs no re-enrolment. `nodes/registry.ts` is where the socket and the provider meet so neither knows the other exists. **The provider** (`providers/nodes/provider.ts`): one offering per *enrolled* node — connected and undrained, what it can still run at `hourlyCents: 0` and `lan` from its own hello; every other node **zero, and it stays in the list**, because "the venue exists and is not answering" is a different fact from "there is no venue". Ports are the lowest free game/GOTV pair from 27415 up per node; the address players are told is the `address` label else the peer address the node's own socket came from. **The warm pool is the orchestrator's to fill** (T11 said so): a warm instance is a server with a server token and only this process mints those, so each warm container gets a ledger row of its own — no match, cost 0, charged to the key that enrolled the node — plus a token minted against it, and it dials `/link` and sits idle. **The one additive migration this task needed is exactly that**: `0002_node-enrolling-key` adds `nodes.enrolled_by_key_id` (nullable, FK), because a ledger row is always somebody's and a warm container exists before any match asked for it; a node with no enrolling key warms nothing and says so once. **A claim is where two rows meet, and the design decision of the task:** `allocate` prefers a free warm instance, `configure` **re-points its token** (`reassignServerToken`, a new store verb) at the walk's row and closes the warm row `released` "claimed by match …", `start` rebinds the live link session and sends the assignment down the socket the container is already holding. The token moves rather than being replaced because there is no way to hand a running CS2 server a new credential; the walk's own freshly minted token rides on the row unused and dies with it. The upshot is one open row per container, always, charged to whoever is using it — which is what makes a node match count against that key's concurrency ceiling even though it costs nothing. `ServerLink` gained `rebind(server, fleetServerId, matchId)` for that (the session's row and match were `const`; `assign()` already existed for T12). **The assignment is sent off the walk's chain, tracked, never awaited inside `start`** — composing one runs `enqueue` on the match's chain and the walk that called `start` is still holding it, so awaiting would deadlock (the T3/T10a lesson, found by reading rather than by hanging); `NodesProvider.settle()` makes that work visible to a barrier. A node that drops off the wire does not end its matches: its containers keep running, the provider keeps listing them so the reaper does not call a live match lost, `fleet.node_disconnected` goes into every match it was holding, and only after `NODE_LOST_MS` (a minute) does `status` say `gone`; after a restart of *this* process the provider rebuilds its instance map from the open ledger rows on the node's first `hello` (`findLiveServerToken`, the other new store verb), for the same reason. Store work: `NodeRow`/`NodeEnrolmentRow` with insert/find/list/update/by-token-hash/use-enrolment, `reassignServerToken`, `findLiveServerToken`, all across memory and Postgres. The five `/v1/fleet/nodes` routes are served (T2's honest `internal` stubs replaced), a `node-links` step joined the drain **after** `links` so a container's last `state` frame still lands on a link this process is listening to, and `EZPUG_IRON_NODE_SERVER_IMAGE` decides what a node runs (`ghcr.io/ezpug/ezpug-iron/cs2:dev` by default). **The dev world, and one place the task summary lost to CLAUDE.md:** `pnpm dev:up` now calls `scripts/dev-node.sh auto`, which runs this box as a node — mint a fleet key, `POST`, enrol, run the agent in the background — but only when the orchestrator is up *and* `EZPUG_IRON_PROVIDERS` names `nodes`, and prints why it did not otherwise; `.env.example` keeps `sim` with `sim,nodes` commented beside it. Registering a real provider takes the sim out of selection for every request that did not ask for it (`providers/selection.ts`, deliberately — a match nobody asked to simulate must land on iron), so making it unconditional would have broken "offline-first" for a fresh clone. `pnpm dev:node up|down|status|logs|forget` is the rest of it, and it exports the `EZPUG_NODE_*` the agent needs rather than asking an operator to refresh a `.env` that predates T11. **The fake node** `@ezpug/protocol/fake-node` is `fake-server.ts`'s twin (no timers, no backoff, no clock — a test drives it) and is what every orchestrator test connects. **Proven on this box, for real, not simulated:** the dev orchestrator with `sim,nodes` (`/healthz` reporting both providers), `pnpm dev:node up` enrolling `devbox` over a real socket and being welcomed, `GET /v1/fleet/nodes` and `/v1/capacity` showing 2 free LAN slots in `saarland`, then a `requirements.lan` match → the node started a real `cs2:dev` container on 27415/27416 → CS2 booted → the plugin dialled `/link`, took its assignment and reported `server_ready` → the match reached **`ready`** with connect facts and a ledger row `running` on `node: devbox` at 0 cents → cancel → the container removed by the node → no open row. Then `pnpm dev:node forget` (revoked at the orchestrator and here), the check's key revoked, `.env` back to `sim`, no container and no process left. That is the first time a match request has become a CS2 server through a node; T13 is the one that plays it out with bots. pnpm verify green (orchestrator 218 passed / 6 skipped incl. 19 new in `nodes/nodes.test.ts`, node 61, protocol 4 files, C# 32 core + 113 SDK, boundaries clean), pnpm verify:extended green (the conformance against the real service 12 passed / 3 skipped, twice, ~140 s each). — cccc52b — 2026-09-06
T13: bots play a real match — the recorded fixtures. `scripts/iron-match.mjs` (`pnpm iron:match`) plays a whole `pug` on the dev node through nothing but the Match API and writes down everything it said: mint an admin key from the box → mint the run's own key with a webhook secret nobody else holds → open a loopback webhook endpoint that **verifies every delivery with the published verifier before writing it down** → presign a PUT into the platform's dev MinIO (SigV4 by hand, forty lines, no SDK) → `POST` a `pug` with `requirements.lan` → subscribe to the stream → fill the bots → force the start → wait → replay the events route page by page → read the ledger. Its key is revoked and its server released in a `finally` **and on a signal**, and because a recording costs ten minutes of real hardware every run also writes `raw.json`, so `--rebuild <dir>` writes every fixture again without playing another match (used five times while the fixture *shapes* were settled). The two conversations a client cannot see come from a new, dev-only **trace** in the orchestrator (`EZPUG_IRON_TRACE_FILE`, `src/trace.ts`): one scrubbed NDJSON line per `/link` frame, per `/node` frame and per MatchZy payload, tokens and presigned queries replaced *as the line is written* rather than when it is read, off unless the variable names a file and **refused outright under `NODE_ENV=production`**. **Four things a real server taught this repo, none of which any test could have caught:** (1) **`//` starts a console comment**, so `matchzy_remote_log_url http://host/path` reached MatchZy as the single argument `http` — "Invalid URL: http", the door never wired, and every MatchZy event of that match lost silently; `MatchZyRemoteLog` now quotes all three values. (2) **`matchzy_enable_tech_pause "true"` is refused by the engine** (`String 'true' can't be converted to Boolean`): MatchZy declares that one as a `FakeConVar<bool>` while its other switches are `[ConsoleCommand]` handlers using `bool.TryParse` — where `1` is silently a no-op. The pug manifest's value is `1`; there is no rule to infer and `docs/gamemodes.md` now says so. (3) **The bots have to be standing before the match starts**: MatchZy's `warmup.cfg` runs `bot_kick; bot_quota 0` and its `live.cfg` ends with `mp_warmup_end`, which on an empty server ends nothing — the engine stays in warmup and the match never plays a round. The script fills during warmup, dwells, then sends `css_start`; the dwell is honestly a dwell and not a barrier, because a bot emits no `player_connected` (the SDK suppresses it — the vocabulary's players are people). (4) **A drawn Bo1 does not end the series**: MatchZy's `HandleMatchEnd` reports `remainingMaps: 1` and replays the map, so overtime is on by default — and overtime is not a cure, because CS2 works its overtime clinch out from the `mp_maxrounds 24` MatchZy's own `live.cfg` sets rather than the four the config re-applies a second later, so a bots match can wander past the point either side has won (seen twice in six runs, once to 5–7). `--max-live-minutes` (12) is the wall in front of that: the match ends `force_ended`, the row closes, and the summary says which of the two happened. **The translator gained one rule from the recording**: a `round_end` whose score has not moved since the last one is a repeat and is dropped — MatchZy sent round 1 twice, a second apart, with two different `reason` codes, and a durable log holding round 1 twice is one a client cannot count with. **The fixtures.** `packages/protocol/fixtures/recorded/real-pug-{link,node,matchzy}.json` (the `/link` exchange, the `/node` exchange, and MatchZy's wire **and only its wire** — what it *means* is the orchestrator's, and a copy here would go stale); `packages/match-api/fixtures/recorded/real-pug-bo1.json` in the conformance recordings' own shape (`flow`, `calls`, `envelopes`, `deliveries`, `frames`), which the **C# generated types round-trip byte for byte** in `ProtocolRoundTripTests` — the first time this repo's two languages have agreed on a real match rather than on a table. **No `position_tick` is in any of them, by construction**: decision 6 makes ticks stream-only, so the recorder drops them *and the acks they earned* before anything is written (3 828 of 3 883 stream frames, 3 714 of 3 855 link entries — nine tenths of the bytes, none of the meaning), and both fixture tests assert no file names one. T9's fourteen `schema` placeholders are gone; seventeen files replace them under **three honest provenances** — `recorded` (bytes a real MatchZy sent, carrying the story's state from one file to the next because the round winner is a delta), `derived` (a recorded payload edited into a case one bots match on one map cannot produce, naming the recorded sibling and its own starting score), `upstream` (an event our flow never produces — the veto trio, `demo_upload_ended`, `player_disconnect` — shape read off MatchZy's source, asserted only to be dropped). They are **generated, not typed**: `pnpm --filter @ezpug/orchestrator fixtures:matchzy` runs the translator and records what it said, and `translate.test.ts` then asserts it still says it, so a rule that moves is a diff a reader can argue with. **The `EZPUG_CS2_TESTS` lane** (`cs2.extended.test.ts`) shells out to that same script — one flow, not two — and asserts the summary: `ended`, MatchZy went live, rounds played, `match.ended` reached the client, `series_end` + `map_end` when MatchZy finished it itself, **the ledger row closed and no server left running**; the link and the door are proven by events only one of them can produce (`server_ready`/`player_death` vs `going_live`/`round_end`) rather than by trace counts, which a test never turns on. Opt-in twice over: nothing runs unless `EZPUG_CS2_TESTS` is set, `=required` turns a missing dev node from a printed skip into a failure, and `pnpm verify` never runs it. **Two dev-script defects fixed because they blocked the first run:** `dev-node.sh` minted its key under a name that is unique while it lives and could not revoke it, so a second `up` after a `forget` died with a 401 (the name now carries the moment), and its `EZPUG_NODE_STATE_DIR` was relative, so the agent wrote the identity under `apps/node/` while the script looked at the root and `up` was never idempotent. **The script itself grew two client manners the failures earned:** a `429` is waited out on `retry-after` rather than thrown (the run that did throw gave up in the middle of its own cleanup and left a CS2 container standing), and the events route is paged until an **empty page** rather than a null `nextCursor` — the route deliberately keeps the cursor non-null while a match is live so a client can tail it, and a run that gave up on a live match spun on one cursor until the rate limiter stopped it. **Left to T21, written under the task: `demo.uploaded`.** Nothing uploads a demo yet; the core plugin owning the upload and the orchestrator relaying `demo_available` is T21 whole, and building half of it here would have been bundling. The script already mints the presigned PUT and passes it as `callbacks.demoUploadUrl`, so T21 is a plugin and a relay, and it also owns the lane's `demo.uploaded` assertion. **Honesty about the money rule:** three CS2 containers outlived their run during this iteration — one when the dev orchestrator was force-restarted mid-match, one when the 429 killed the cleanup, one when a run was cut short — each on this box at zero cents, each removed and its ledger row closed within minutes, and each is why the signal handler, the 429 back-off and the paging fix exist. Nothing was left running afterwards (`GET /v1/fleet/servers` empty, no container). Proof: **six real matches** on the dev node this iteration, three of them decided by MatchZy itself; the recorded one ended `completed` 1–3 with a side swap, 52 envelopes, 5 `round_end`, `map_end`, `series_end`, one ledger row closed at 0 cents. `pnpm verify` green (orchestrator 230 passed / 7 skipped, match-api 308, protocol 37, node 61, sim 78, core 55, C# 32 + 114, Biome 328 files, boundaries clean), `pnpm verify:extended` green, and the **`EZPUG_CS2_TESTS=required` lane green** (443 s, `completed`, no server left). Docs: `docs/operations.md` "One real match, recorded" (the three console lessons, the trace, why no tick reaches a file, the lane), `docs/gamemodes.md` (the recorded fixtures, the repeat rule, "writing a cvar a console will accept"), the fixtures' README, README quickstart, `.env.example`. `.env` on this box stays `sim,nodes` with the node enrolled — T14 wants it.
T14: a server that dies comes back. **The orchestrator recovers by walking again.** `match/machine.ts`: the provisioning loop is now `walk(row, until, restore?)`, shared by `provision` and the new `recover` — a live match whose server the probe finds `gone` says `match.recovering` with the newest backup's round, closes the corpse `failed` (released over the link if it still listens, stopped and deallocated on the spot, the reaper behind that), clears presence, and queues the replacement walk behind the step that noticed (`kickRecovery`, once per loss); the walk hands the backup to the provider's `restore` verb where that is its way (the sim loads the story point) and `assignment()` puts it in `assign.restore` for every server whose plugin does the loading (a node today, Dathost when T16 exists). The match stays `recovering` throughout: the window (5 min from the loss) is the deadline for the replacement's `server_ready`, which re-announces the connect facts as `match.server_ready` with **`restored: true` and `round`** and re-arms the join deadline (20 min → `failed: server_lost` "no going_live … of the replacement being ready"); the replacement's `going_live` says `match.recovered` (`resumedFromRound`) and the match is `live` on its second server. No backup, an exhausted list, a replacement that dies too: `server_lost`, everything recorded kept. Late events from the dead server are rejected by handle. `resume()` after a restart with the window open re-arms what fits (a running replacement waits for players, a booting one has the window, none at all re-runs the walk) and the `restore` command is the door for exactly that gap — `invalid_state` "a restore is already in progress" while the orchestrator's own restore runs, which is always unless a restart left no walk, `no_backup` for a round nobody wrote. **Backups have one door:** `ServerEventSink.backup(source, backup)` on the machine (newest eight per match), which the link's `backup` frame now calls instead of writing the store itself, and which the sim provider calls after every `backup_written` with a small stand-in file — a simulated server has no file and until now the sim's matches had **no backups at all**, so the honest-failure test was the only recovery the sim could show. `SimProvider.setFaults({ crash: { afterRound, backup } })` is the conformance suite's crash door, the same knobs the published fake takes: the `server-crash` scenario after that round, `backup: false` a box that keeps its files. **Contract, one additive release:** `@ezpug/match-api` 0.3.0 — `match.server_ready` gains optional `restored: true` + `round` on the replacement's announcement (the task text names them; a consumer can tell a return from a first boot without consulting state), the fake says them too, `crash-restore.json` re-recorded with exactly those two lines changed, CHANGELOG line, `docs/match-api.md` row. **The plugin side (C#):** `GamemodeRuntime.OnAssign` starts the context at the backup's map and round; `GamemodeLoader` goes to the backup's map (not the plan's first), and after `matchzy_loadmatch` + the remote log writes the file into `MatchZyDataBackup/` **with this server's remote log put back inside** (`MatchZyBackups.WithRemoteLog`, the inverse of T9's scrub — read off `BackupManagement.cs`: MatchZy's restore deserialises `match_config` from the file twice, at `matchzy_loadbackup` in warmup (pending) and again at `HandleMatchStart`, so a console re-point alone would lose the token and every match-flow event of the resumed match would knock at a door that refuses it), runs `matchzy_loadbackup <file>`, re-points once more, emits `backup_restored` as the `plugin_event` the simulator speaks; `going_live` stays MatchZy's, which is what closes the window. MatchZy pauses after a restore by default (`matchzy_pause_after_restore`) — left as it ships, documented: players reconnect into warmup, ready up, find their round, `.unpause`. A file name that is not a bare `.json` is refused; a non-matchzy flow warns it has no round backups. **Proven offline three ways:** the sim (`machine.test.ts` "recovery": the full flow with the round list `sim-1#1, sim-1#2, sim-2#2, sim-2#3, sim-2#4` and the ledger `failed`/`released`; the join deadline on a parked replacement; `restore` refusals and the post-restart door; `resume` re-running the walk — the test app's sim sink became swappable for a revived machine), the fake node (`nodes.test.ts`: the container's socket dies and the snapshot forgets it while the agent keeps heartbeating → after three silent intervals the probe says `gone` → `stop` for the corpse, a second `start` with a fresh token, the replacement's `assign.restore` equal to the backup frame the first container sent, `match.recovered` on its `going_live`, one open row), and the conformance suite: `crash-restore` and `crash-lost` no longer skipped — green in process **and against the real service** over Postgres/Redis on the system clock (62 s and 36 s, the loss detectors being real thirty seconds). The fake Dathost's "server vanished" is T15's to add; the machine's path is provider-blind (`status` → `gone`). Found on the way: the extended suite's namespace sweep deleted matches with `backups` rows hanging off them (an FK nobody had hit because no sim match ever wrote one) — the sweep clears them first. Docs: `docs/operations.md` "Recovery" rewritten, the deadlines table (join, again), `restore`, the sim's backup stand-in and `setFaults`; `plugins/README.md` "A restore"; `docs/sdk.md` host hooks; `docs/nodes.md` the vanished container. pnpm verify green (orchestrator 225 + 1 skipped incl. 4 new recovery tests and the node one, match-api 308, C# Core 42 + SDK 115, Biome), pnpm verify:extended green (239 passed / 3 skipped; the extended conformance 12 tests, `crash-restore` 56 s and `crash-lost` real seconds included; release check ok). The `EZPUG_CS2_TESTS=required` lane ran on the dev node with the image rebuilt from this tree (new plugin, the host dev orchestrator still the previous code — the orchestrator side is the extended tier's): 4 passed, a whole `pug` `completed`, no container left. Not run on real hardware: a kill mid-match on the dev node — that is T37's rehearsal; the fake node proves the node path here. — 1935be7 — 2026-09-06
T15: the fake Dathost. `apps/orchestrator/src/providers/dathost/fake.ts` — a Hono app over `/api/0.1` speaking the vendored subset and nothing else (create, list, get, update, delete, `duplicate`, `sync-files`, `start`, `stop`, console GET backlog + POST line, files list/download/upload/move, `metrics`, `account`; the vendor's *match* API is not there and never will be — Match.md §3 chose the raw server API so a Dathost server and a LAN node behave identically). Its door is a **`fetch`** — the shape `createDathostProvider({ fetch })` takes in T16 — with `listen()` beside it for anything that needs a URL; an abort on the caller's signal rejects the way real `fetch` does instead of reaching a handler, which is what makes the console-timeout fault a timeout and not a 500. **Four vendor behaviours modelled because the adapter is wrong without them:** `duplicate` copies the **last synced** files (every server keeps two file sets, what is on the box and what the API cached; `sync-files` and `stop` refresh the cache, `duplicate` reads it — upload-then-clone gets yesterday's plugin here exactly as it would there, which is T18's trap); `start` **reboots** a server that is already on, and a boot is `bootMs` of *clock* time; the **single GET refreshes `booting`, the list does not** (which is why `status()` reads the single item); `deletion_protection` **blocks DELETE** (the template carries it, a clone comes back off, unprotected and tagged with `duplicate_source_server`). Multipart in, dotted field names merged into their settings object (`cs2_settings.rcon`) and the form's text coerced to the schema's booleans and integers. Faults on `setFaults`: 429/500 for the next N requests, optionally narrowed to a path; a boot that never ends; a server that vanished (404 everywhere, absent from `list` — what T14's recovery sees when a box dies); a console read that never answers. Nothing random and no wall clock: ids off a counter, every instant off the injected `Clock`, and the RCON/join/FTP/MySQL passwords it invents are obviously synthetic (`fake-…`) so a recording that ever caught one is greppable. **The proof:** `fake.test.ts` wraps *every* request the suite makes in `call()`, matches its path against the merged OpenAPI's path templates, asserts the status is one the document declares, and validates the JSON body against the schema it names — ajv (a new catalog entry, test-only, nothing shipped imports it) over `references/dathost/openapi.merged.json`, **read from disk rather than imported** (CLAUDE.md: `references/` is never imported; `references/dathost.md` already names that file the fake's schema truth). The document's own gaps are listed explicitly rather than passed over: the statuses it never mentions (401 Basic auth, 403 deletion protection, the injected 429/500), the one operation missing a 404 every sibling documents (`GET …/metrics`), and the two bodies nothing describes at all — `GET …/console` → `{lines: […]}` and `GET …/files` → `[{path, size}]`, which T19's live smoke is where the real API gets to correct. `POST /game-servers` has no schema either, but its page says it "returns the new server in the same format as game-servers", so it is held against `GameServerOutput` anyway. 21 tests. pnpm verify green (19/19 tasks: orchestrator 260 + 3 skipped, match-api 308, node 61, sim 78, protocol 37, core 55, C# Core 42 + SDK 115, Biome and the guards). Not the extended tier — T15 is not on the working rules' extended list, the one test that opens a socket binds an ephemeral loopback port and closes it in `finally`, and no provider is touched. **No real server was allocated and none exists to leave running:** the first live Dathost call of this round is T19's demanded smoke. — 0777402 — 2026-09-06
T16: the Dathost provider. `apps/orchestrator/src/providers/dathost/provider.ts` — the vendor's raw server API behind the provider interface, registered iff the account is configured. **The PRD's one-sentence provisioning mapped onto the walk's three verbs:** `allocate` = `sync-files` on the template (at most once per 10 min of clock, so the vendor's "duplicate copies the *cached* files" caveat cannot be reached by forgetting) → `duplicate` → one `PUT` planting our identity (readable name, the ledger tag in `user_data` as JSON, `autostop`/`reboot_on_crash` off, a **freshly minted RCON password** so no two clones share the template's, the GSLT lease); `configure` = the two facts that only exist once the walk minted them — `cs2_settings.password` and `ezpug.json` (link URL + server token) through the files API, nothing else, because the assignment travels over the link; `start` = `POST …/start`. **What it deliberately does not write:** `cs2_settings.slots` and GOTV are the template's — slots are part of a pay-as-you-go price and the ledger snapshots `cost_per_hour` at allocate, so a clone that re-priced itself would make the budget a fiction; `tv` is *read* from `enable_gotv` (the vendor's schema has no `tv_delay`, so the relay's delay is our constant, 90 s, matching the image). `status` reads the single GET (the list does not refresh `booting`): booting → `starting`, on → `running`, 404 → `gone`, off → `stopped` — which during a live match is exactly the loss the probe hunts — except between `allocate` and `start`, where this process knows better and says `allocated`. **Money rules, each with a test:** a `duplicate` is **never** retried (a repeated clone is a second server on the bill) while a 429 is retried everywhere and a 5xx/dropped socket only where repeating is safe, all backed off on the injected clock (4 attempts, 1/3/9 s, `Retry-After` honoured to a 60 s cap); an allocation that fails after the clone exists deletes the clone before the walk moves on ("leaves nothing behind"); a clone it could not delete is still claimed by `list()` — `duplicate_source_server` is a tag the vendor writes itself — so the reaper takes it instead of billing quietly. `list()` claims our tag, **or** untagged-and-cloned-from-our-template, and **never** another deployment's tag: two orchestrators may share an account and a reaper that deleted the other's live match would be worse than a leak. Every verb refuses the template id outright. The Basic header is built once and never logged, never in an error, never in `provider_meta`; the RCON password lives in memory only (nothing downstream needs it — the console door is Dathost's, not RCON). T17's pool is a two-method seam (`lease`/`release` keyed by the Dathost server id, the vendor's "one token per running server"); absent, the server is LAN-only and it says so once. Env: `EZPUG_IRON_DATHOST_{EMAIL,PASSWORD,TEMPLATE_SERVER_ID,LOCATION}` (the repo's prefix, already in `.env.example`), with the PRD's unprefixed `EZPUG_DATHOST_*` as aliases — all three or none, half a set fails at boot naming what is missing, none with `dathost` in `EZPUG_IRON_PROVIDERS` is a startup warning and a fleet on `sim,nodes` (T35's deploy). `docs/operations.md` gains a Dathost section (the verb table, the money rules, the shared-account caveat). 29 tests against T15's fake, faults included (429 backed off, 5xx retried, 5xx on `/duplicate` not, a console that never answers timing out on the clock, a vanished server, a wrong password refused unretried and never echoed). `pnpm verify` green and `pnpm verify:extended` green (exit 0) on the final tree. **No real Dathost call was made and no server exists to leave running:** the first live call of this round is T19's demanded smoke. — 123ba5f — 2026-09-06
T17: the GSLT pool. A CS2 server started without a Steam Game Server Login Token logs in anonymously and takes **LAN connections only**, so every rented server needs one and a node at a venue needs none — which is why only the Dathost provider holds the seam. `apps/orchestrator/src/gslt/`: `steam.ts` (the four `IGameServersService` calls — `CreateAccount`, `GetAccountList`, `ResetLoginToken`, `DeleteAccount` — on the injected clock, 429/5xx backed off 1/3/9 s, a 403 named as "the key is wrong" and asked once, the key in a form body on every write so it is in no URL and in no error), `fake-steam.ts` (the same four in process, Steam's `{"response": …}` envelope and its plain-parameter key modelled, tokens `fake-gslt-…` so a recording that ever caught one is greppable; the pool's tests, `createTestApp`'s world and a dev box all mint against it), `pool.ts` (lease, release, stats, sweep). **The lease is keyed by the ledger row, not by the Dathost server id** — a change to T16's two-method seam, because the row exists before the clone does and outlives it, so no clone created-and-never-named can strand a lease and the sweep can free every lease whose row has closed; `servers.gslt_token_id` (T2's column, filled at last) says which row holds which account. Five rules, each a test: **one token, one running server** (Valve evicts the first login, so the claim is one atomic `update … where id = (select … for update skip locked)` and the pool hands out the *longest-idle* free account — `leased_at` is kept after the release for exactly that ordering); **the ceiling is a wall, not a target** (nothing is minted until a lease finds the pool dry, `CreateAccount` is never retried for the same reason `duplicate` is not, `EZPUG_IRON_GSLT_POOL_MAX` default 16); **a dry pool is a warning, never a refusal** (the lease answers null, the provider says "LAN connections only" once, the match still runs); **a lost server's token is reset** — the Dathost provider now passes `{lost: true}` when the `DELETE` found the box already gone, since nothing proved it stopped and it may still be logged in, and a reset that fails keeps the account because the old token still works; **a crash cannot leak a lease** (a five-minute sweep, on the chain with the leases, frees every lease whose ledger row is closed or absent, then reconciles `GetAccountList`: an account Steam forgot is dropped, one Steam expired is reset, one carrying this deployment's memo `ezpug-iron <public host>` that the database does not know is adopted — two deployments may share a Steam key and neither ever touches the other's memo). `GET /v1/fleet/gslt` is served at last (`{total, inUse}`, `fleet` scope, never a token). Env: `STEAM_WEB_API_KEY` (PRD's name; `EZPUG_IRON_STEAM_WEB_API_KEY` the prefixed alias), `EZPUG_IRON_GSLT_POOL_MAX`, and `EZPUG_IRON_STEAM_FAKE_TOKENS` — the dev pool against the in-process fake, refused under `NODE_ENV=production` and refused beside a real key (one of the two would be silently ignored). No key at all is not a boot failure: the pool leases the rows it holds and mints nothing, which is every developer's box. **No migration** — T2 designed `gslt_tokens` and `servers.gslt_token_id` in the round's first one, exactly as the budget note hoped. Store: six methods on both implementations, proven by the contract test over memory **and** Postgres. `docs/operations.md` gains a GSLT section (the rules, the Saturday triage: `total: 0` is no key or a wrong one, `inUse == total` is a saturated pool). 36 new tests; `pnpm verify` green (19/19) and `pnpm verify:extended` green (19/19, the conformance suite against the real orchestrator included, CS2 lane skipped with its printed reason). **No Steam account was created and no real server was allocated:** every mint in this task was against the in-process fake. — 5711bda — 2026-09-06
T18: the template image script. `scripts/dathost-image.mjs` — Dathost has no image registry: a server there is a box with files on it, and `duplicate` is the only way to get a second one carrying our plugins, so the "image" for the rented half of the fleet is **one server that never runs a match** and this is what builds it. **One build, two destinations, literally.** The files it uploads are not fetched again from Metamod's or MatchZy's release pages: they are read out of `ghcr.io/ezpug/ezpug-iron/cs2` itself — `docker create` + `docker cp /opt/ezpug`, the directory T10's entrypoint overlays onto `game/csgo`, no container ever started, cached per image id under `.cache/` — and every version comes off `docker/cs2/Dockerfile`, the *home* whose numbers `docs/pins.md` copies and `pnpm lint` keeps honest, so nothing is decided twice. The path mapping is the entrypoint's (`addons/**` and `cfg/**` where they are, `gamemodes/<id>/cfg/**` into `cfg/`, two modes wanting one cfg path refused by name rather than settled by whichever `cp` ran last), and the gamemode **manifests are deliberately not uploaded**: they travel over the link inside the assignment (decision 5). 509 files, 163.3 MB on a first build against the real image. **The record is what makes a refresh free.** The last file written is `ezpug-image.json` at the game root — the pins, the plugins' own `build.json` revision, a sha-256 and a size per file, no secret in it. A second run uploads nothing; a file edited through the control panel is caught by the *listing's* size disagreeing with the manifest, and the one edit neither can see (same byte count) is what `--force` is for. Then `sync-files`, **always last**, because `duplicate` copies the API's cache and an upload without a sync is a template that clones yesterday's plugin — which looks exactly like a plugin bug; the test duplicates the template afterwards and reads the plugin off the clone. **What the template is made to hold**, each with its consequence: `deletion_protection` on (it is the image, and it is also what stands between a misconfigured deployment's tag-based reaper and a rebuild from scratch), `autostop`/`reboot_on_crash` off, Dathost's *managed* Metamod off (ours is pinned and checksummed in the image; two loaders are one too many), GOTV on (T16 reads the relay off a clone and never turns one on), and **never a GSLT** — one token per running server (T17), every clone inherits the template's, so a token found there is cleared and `--check` is red on it. `cs2_settings.slots` is set on create (`--slots`, default 12) and left alone afterwards: slots are part of `cost_per_hour` and the ledger snapshots that at allocate, so re-pricing the template quietly would make every budget a fiction. **`gameinfo.gi`.** Metamod loads because that file says so and a game update rewrites it; in the container the entrypoint re-checks it every boot, on Dathost nothing does — so the script adds the loader line at the same anchor the entrypoint uses and `--check` goes red when it has gone (a whole fleet booting with no plugins is the symptom). A file root that carries no `gameinfo.gi` at all is a warning naming T19 as where the real file manager's layout settles, not a failure. **Refusals**, since "refuses to touch a server that is not the template" is the sentence the money hangs on: every write is preceded by a GET and the `user_data` marker (`{"tag":"ezpug","role":"template"}` — a test asserts `TEMPLATE_TAG` equals the provider's `DATHOST_DEFAULT_TAG`, because the reaper deletes by it); a match's server and a clone are refused **even with `--adopt`**, an unmarked server is refused with `--adopt` named as the deliberate way in, a running template without `--force`, an id not on the account, and a tree whose `build.json` disagrees with the pinned CounterStrikeSharp stops before the account is touched at all. `POST /game-servers` is never retried on a 5xx (a second server on the bill nobody knows about) while a 429 is, everywhere. The password comes from the environment or `.env` only, never a flag (a flag lands in shell history and `/proc`); the Basic header is built once and is in no log line, no error and no `--json` body. **The proof:** `apps/orchestrator/src/providers/dathost/image-script.test.ts`, 31 tests against T15's fake with a hand-written six-file tree — the script is imported as a module (`main({argv, env, fetch, sleep, now, stdout, stderr})` returning an exit code) so the suite needs neither a subprocess nor docker: the dry run that touches nothing, the create, idempotence down to the two calls a no-op run still makes, the re-upload of a file edited behind its back, `--force`, `--check` red on a drifted file, pin, setting or `gameinfo.gi`, and the clone that carries the plugin. Manually once on this box, recorded here because no test does it: the real image extracted and driven through the fake end to end — 509 files in 1.2 s, `--check` in 0.5 s, 510 files on the template. `pnpm verify` green (19/19) and `pnpm verify:extended` green (19/19 + 3/3, the conformance suite against the real orchestrator twice; CS2 and Dathost lanes skipped with their printed reasons). **No live Dathost call was made and no server exists to leave running:** the first call to dathost.net in this round is still T19's demanded smoke. — a047e72 — 2026-09-06
T19: the Dathost live smoke. `scripts/dathost-smoke.mjs` (`pnpm dathost:smoke`) — the one command in this repo that spends money: it rents one server in Frankfurt, waits for the plugin on it to dial the link out of Düsseldorf, talks to it, and gives it back. **Nine steps, and the order is the whole design.** `GET /account` (the credentials are the credentials); T18's `--check`, imported as a module rather than shelled out so a template that is not what this tree builds stops the run *before* a clone exists; `/healthz` only for "is there an orchestrator at this URL at all" — a wrong `--base-url` is the commonest way to run this and must not read as a missing provider — with `GET /v1/fleet/providers` as the authority on whether `dathost` is registered and undrained, because `createHealth` has no provider probes yet (that is T31, and the fleet route already knows); the account **counted before** anything is touched, so a stray afterwards is attributable and a tagged server that was already there is warned about, counted out and never deleted (two deployments may share an account, and a smoke that took somebody else's live match down would be worse than a leak); one match with `requirements.provider: 'dathost'` — not `lan`, not `simulated`, this provider and no other, so a box that also has nodes cannot answer it; the wait for `ready`, which is reachable only by the plugin on the clone saying `server_ready` over its own outbound socket; `ezpug_status` sent as an `rcon` command down that link and its answer read back, which is the link proven in **both** directions with one call; the connect facts and the GOTV relay read off the ready match; and then — deliberately **outside** the `try`, after the release — the ledger row asserted closed and the account counted again, because a run that died in step 6 has exactly as much to prove there as one that reached step 8. **The money**, since that is what the task is really about: exactly one server is ever allocated; the release is a `finally` that also runs on SIGINT/SIGTERM; the run is bounded by `--budget-minutes` (default 60, the PRD's server-hour) and the match carries `ttlMinutes: 60` so the reaper takes the box back even if this process is killed; and if a clone is still on the account when the run ends the script deletes it itself, says it had to, and exits non-zero. The run mints its own key with a one-server ceiling and `--budget-cents` (default 500) — **`monthlyCents: 0` is "no money", not "no limit"** (T5), which cost the first green run of the rehearsal — and revokes it *last of all*, after the two post-release assertions, which need a key that still works. No secret reaches the summary: `connect` is a host, a port and `passwordSet: true`, never the password; the API key, the server token and the Basic header are in no line of it, and a test greps for each. **The rehearsal is what makes this reviewable at all** (`providers/dathost/smoke-script.test.ts`, 12 tests in `pnpm verify`): the same `main({fetch, env, sleep, now})` driven through all nine steps against the fake vendor (T15) behind a *real* Dathost provider (T16) in a *real* orchestrator, a template built by the *real* image script (T18) from a six-file tree, and the *real* link on a real port with `@ezpug/protocol/fake-server` dialling in the moment `configure` plants `ezpug.json` on the clone — the script's injected `sleep` is the seam: it dials the plugin in and then turns the app's fake clock, so a poll loop written for a datacentre plays out in two seconds with no `vi.waitFor`. It covers the release after a step-7 failure, a clone the provider could not delete (a 403, not a 500 — a retry backed off on a clock only the script turns would never come round), the server that never dials in, and the secret grep. **What the lane needs that this box has not got**, said plainly: the plugin dials *in* from a datacentre, so `EZPUG_DATHOST_TESTS=required` needs an orchestrator the internet can reach — `gs.ezpug.com` after T35, or a tunnel — on top of the credential trio, and it says exactly that when it has neither. **No Dathost credentials exist on this box and no live call was made: the lane skipped with its printed reason, and the first request to dathost.net in this round is still ahead.** `docs/operations.md` gains the smoke section (the nine steps, the money, the lane's two preconditions), `.env.example` the lane's note and `EZPUG_IRON_ADMIN_KEY`. 14 tests added; `pnpm verify` green and `pnpm verify:extended` green (the conformance suite against the real orchestrator included; CS2 and Dathost lanes skipped with their printed reasons). — 85ea927 — 2026-09-06
T20: RCON and the console. The two operator doors that had answered `notServedYet` since T2 — `GET /v1/fleet/servers/:id/console` and `POST …/rcon` — and the Source RCON client behind them. **Our own framing, not `rcon-client`.** Read the package first, as the task said to: ~200 lines whose whole substance is Valve's four-field packet plus a `setTimeout` this repo may not use (every deadline is the injected clock), so `apps/orchestrator/src/rcon/` carries `protocol.ts` (encode/decode, a partial tail finished by the next chunk, a length prefix that is not a packet), `client.ts` (one connection per command — RCON has no session an operator's occasional line would amortise, and a socket we do not hold cannot leak; the pre-verdict noise packet skipped and bounded, the empty `RESPONSE_VALUE` sentinel that ends a multi-packet answer, `RconError` with `unreachable | auth_failed | timeout | protocol`) and `fake-server.ts` — a real TCP listener speaking the real protocol, because framing is only right if something else reads it. **The resolution order, and why RCON's is not the obvious one.** The console reads the plugin's own relayed tail first (soliciting one when the session has never cached one — an operator opening a console wants the console), falls back to the provider's control-plane backlog (Dathost keeps a log; a node keeps none, the container *is* the server), and answers `{lines: []}` rather than an error when nobody has anything to say. RCON goes the other way round: the **provider's** `rcon` verb first, because it is the only door that returns what the server *printed* — the plugin can run a line but cannot capture the engine's answer, so it applies it and says nothing — and the link only when a provider has no door at all; a sim is `command_unsupported`, a closed row `invalid_state`, a shut door `provider_unavailable`. Decision 5 keeps its shape either way: RCON is for a human and for the moment before the link is up. **The node's door is the new one.** The `nodes` provider mints an RCON password per container, delivers it in the container's environment (`EZPUG_IRON_CS2_RCON_PASSWORD`, the variable the image already reads) and keeps it in memory only — never the ledger, never a log line — and leaves it `undefined` for a container adopted after a restart, where the honest answer is that there is no door rather than a guess; `scrubTrace` learned the variable name so a recorded fixture cannot carry one. **Nothing from either route carries a credential:** `rcon/redact.ts` masks the value after any password-ish cvar (`rcon_password`, `sv_password`, `sv_setsteamaccount`, MatchZy's remote-log and remote-backup header values, a presigned upload URL) in every line served *and* in the ledger's `rcon_audit`, the column T2 designed for exactly this — appended and trimmed to the last 200 in one SQL statement, so two operators typing at once both leave a trace rather than one overwriting the other. No migration and no `@ezpug/match-api` change: the routes and their schemas already existed, this build simply serves them. `docs/operations.md` gains "The console and RCON" and corrects the schema note about who holds an RCON password. 26 tests added (13 on the client against the real listener, 10 on the routes, 2 on a node's door through the whole HTTP surface, 1 on the trace scrub, plus the audit's assertions in the store contract over both stores). `pnpm verify` green and `pnpm verify:extended` green. **One P1 raised, T20a:** the SDK's `LinkClientTests` backoff test failed once in an extended run on a box that was also carrying the platform's e2e round — pre-existing, not caused by this task, and traced to three 5 s *wall-clock* safety nets in the C# harness beside a test whose own timeline is the injected clock. Not retried into green; written into the PRD instead. — 28e24d2 — 2026-09-06
T20a: the SDK's link-client harness waits, fixed at the root rather than at the budget. Reproduced the flake deliberately first: with 24 busy loops on this 12-core box (load average ~35), the pre-fix tree failed `LinkClientTests` 1 of 12 runs in a full pass, then on the 6th, 1st and 2nd run of three further loaded passes each stopped at the first failure to capture it — and the captured message named the mechanism: `TimeoutException: the client sent nothing within 5000 ms; it had sent 0 frames`, thrown by an `ExpectHelloAsync` immediately after a `Clock.Advance`. The client had not dialled at all, so this was never latency. `RunAsync` logged "reconnecting in N ms" (LinkClient.cs:267) and only then armed the wait on the injected clock (`WaitAsync`, :268); the test reads that line as its barrier and advances the fake clock, so when a saturated scheduler deschedules the loop between the two statements both `Advance(999)` and `Advance(1)` land before `_clock.After(1000, ...)` exists, the timer is armed at now=1000 due=2000, and nothing ever dials. A bigger budget would only have made it fail slower. Three changes, no assertion touched: (1) LinkClient arms the reconnect wait before logging it, so the line is a promise that the timer exists rather than a prediction that it will — the one production change, four lines and the why; (2) `FakeClock` is thread-safe, because the test advances from its own thread while the socket thread and the reconnect loop arm timers on the same `List` (a `Where`/`OrderBy` walk racing an `Add` is an `InvalidOperationException` or a silently lost timer) — the book of timers is kept under a lock but a callback fires outside it, since `OnWelcome` arms the heartbeat while holding the client's own gate and `Heartbeat()` takes that gate when it fires, which the naive lock would have closed into a cycle; a timer cancelled between the pick and the call now stays cancelled; (3) the harness's wall-clock bounds move from 5 s to a documented `EZPug.Sdk.Testing.Patience.TimeoutMs` (30 s), the C# twin of `eventually()`/`EVENTUALLY_TIMEOUT_MS` carrying the same two rules in its doc comment (never for a negative; a budget is not an ordering), applied to `ScriptedLinkSocket.NextSentAsync`, `Rig.LoggedAsync`, `Rig.WaitProcessedAsync`, the same wait in `LinkClientFixtureTests` and the `SystemClock` wait in `FakeClockTests`; xunit configures no per-test timeout to stay under. `ScriptedLinkSocket.Sent`, `ScriptedLinkSocketFactory.Sockets` and the rig's `Log` now hand out snapshots taken under the writer's lock instead of the live `List` the assertions were reading across threads, and the factory's refusal queue is concurrent. Proof: the whole plugin suite (42 + 115 tests) ten times green with the 24 loops still running at load average 31-35, then 25 further loaded runs of `LinkClient*` alone — 0 failures; `pnpm verify` green after the loops were killed (19/19 tasks, 403 TS tests, 157 C#). No doc pinned any of the changed numbers. — e2b3b17 — 2026-09-06
T21: demos over the link's shoulder, and the half of T13's lane that waited on it. **The server owns the upload** (decision 10), so the whole pipe is three seams and one new deadline. `EZPug.Sdk/Demos/`: `DemoUploader` hashes the file *first* and reopens it for every attempt — a retry must send the bytes the hash promised, and a stream read once cannot — then streams it at the presigned URL behind an `IDemoTransport` seam (four attempts, 2/6/18 s on the injected clock, never `Task.Delay`), and never throws: a demo that would not go up is an outcome the match reports, not an exception the plugin dies of. `DemoFiles` answers the question CS2 does not: nothing — no event, no forward, no cvar — says a `.dem` is finished, so the newest one is watched and taken for done when its length has not moved for fifteen seconds. `EZPug.Core/Demos/DemoFlow` binds the two to the match: `tv_record`/`tv_stoprecord` only for a flow that has nobody else recording (a `matchzy` flow has MatchZy on its own schedule), the stop **one `tv_delay` after the win panel** because GOTV records the *delayed* broadcast and stopping on the panel would cut the last rounds off the file, and the PUT's continuation hopped back onto the game thread through the world's clock so the emit happens where every other emit does. Its patience is four minutes and the orchestrator's is six, deliberately: the plugin gives up first, so the match ends on a demo that was *announced* rather than on a deadline that expired. **One demo per match**, said in the log rather than silently: the request carries one `demoUploadUrl` and a second map's PUT would overwrite the first map's in the client's storage — a series wants a URL per map, which the Match API does not offer yet. `@ezpug/match-api` 0.4.0, additive: `demo_available` gains `sha256` + `contentType`, present only once the bytes actually landed, so a `demo_available` **without** them is honest news that a demo exists on a server and nowhere else; `match.ended` gains `demo {uploaded, skipped?}` over the closed `DEMO_SKIP_REASONS` (`no_upload_url | not_recorded | no_demo | upload_failed`), and `matchDemoOutcome()` is exported as the *one* rule both the fake and the real orchestrator answer it by, so the two cannot drift. **The new deadline is the subtle part.** A hash arriving is what the orchestrator relays as `demo.uploaded` (with the object key read out of the presigned URL's path — the only thing it can say about where a byte it never saw went), but `series_end` would have ended the match and *released the very server the file was still being written on*. So a match that records a demo and was given somewhere to put it now stays `live` past its own `series_end` until the demo is announced or `deadlines.demoMs` (6 min) runs out — a client watching `Match.state` sees `live` for a couple of minutes after `series_end` reaches its webhook, which `docs/match-api.md` now says in as many words. The sim PUTs its own record for the same reason a plugin does (a simulated box is the server side of the link, not a shortcut through it), and needed an **ordering barrier** to do it: a `demo_available` held for a network round trip would otherwise be overtaken by the `series_end` two beats behind it, the machine would end the match on that one and reject the demo that arrived after — so the story now feeds through a promise tail, each step still registered with T10a's `ingesting` set so `settle()` keeps its meaning. `gamemodes/pug/cfg/ezpug/pug.cfg` moves its bot lines *above* the GOTV block (the engine counts the SourceTV client as a bot). T13's lane loses its `--no-demo` and gains the `demo.uploaded` assertion; `iron-match.mjs`'s force-end wall moves 12 → 15 min to sit above the play *plus* the demo window. 17 tests added (7 on the machine's window and relay, 10 in C# over the uploader's retries and the flow's watcher), the link's `demo_available` round-trip, three conformance checks, `docs/{match-api,operations,sdk}.md` and `plugins/README.md`. `pnpm verify` green (19/19, 410 TS + 167 C#) and `pnpm verify:extended` green; the CS2 and Dathost lanes skipped with their printed reasons in this run (no dev node up). **One P1 raised, T21a, and it is the one thing this task could not prove end to end:** on a real dev-node run the engine kicked GOTV as a bot on MatchZy's own `bot_quota 0` and **no `.dem` was ever written**, so the match ended — correctly, honestly, six minutes after `series_end` — with `demo.skipped: no_demo`. Everything either side of the missing file is proven; the file itself is T21a. Not retried into green. — 50098d5 — 2026-09-06
T21a: GOTV survives the bot purge, and the cause was not the one the finding named. Reproduced T21's run first — a whole match, `demo.skipped: no_demo` — then took the engine apart one command per boot against CS2 1.41.7.8, reading `status` and the container log either side: **a bare `bot_quota 0` does not kick SourceTV.** `bot_quota_mode` decides, and it decides two things at once. Under `fill`, *any* change purges every fake client including GOTV and refills; under `normal`, a quota that **drops** while bots are standing takes GOTV with it while a **rise** does not; under `competitive` — what `gamemode_competitive.cfg` sets at every map load — the purge spares GOTV, **but those bots never fire a shot**: ten of them played seven rounds with zero kills and zero damage between them, every round to the CT side on the clock (read out of the server's own `logs/*.log`, which is where kills go — the console prints none), so each half splits evenly, the map draws 2-2 by construction and the overtime 3-3, and no `series_end` ever comes. Two runs went sixteen rounds at 8-8 before the wall ended them. So the fix is a shape, not a cvar. `gamemodes/pug/cfg/ezpug/pug.cfg` sets `bot_quota_mode normal` **behind a `bot_kick`**: a whole cfg is one frame and the eviction pass runs at the end of it, so dropping the quota and switching the mode above the map's own two bots evicts GOTV with them — measured in this exact file, one line short of the fix, and it is why MatchZy's own `warmup.cfg` opens the same way. `scripts/iron-match.mjs` then **empties the server before `css_start`**, so MatchZy's `live.cfg` opens on a `bot_quota 0` that drops nothing, and asks for its bots once the match is live and the only way left is up; the cost is that `live.cfg`'s own `mp_warmup_end` runs on an empty server, so the script sends one itself a poll later (outside warmup it does nothing, which is what makes that safe). Three smaller findings ride along: `--bots` is a head count again (10, not per team) because the mode is `normal`; `mp_freezetime 5` travels in the match config, because eighteen seconds a round of bots standing still is four minutes of a run that has to finish; and the force-end wall is 35 minutes, not 15, because bots draw a four-round map more often than they win it and one run reached `series_end` eight seconds the wrong side of a 25-minute one. `mp_roundtime 1` was tried and **reverted** — it was blamed for the all-CT rounds that turned out to be the `competitive` bots not fighting, and a change kept on a reason already shown to be something else is not a change. The `EZPUG_CS2_TESTS` lane's demo branch is now "the demo landed" rather than either outcome (`demo_available` 1, `demo.uploaded` 1, `match.ended.demo {uploaded: 1}`), its budget 45 minutes to match the overtime tail, and `docs/operations.md` carries the whole measurement table where an operator will look for it. The recorded fixtures are a real completed match again — 4 rounds, 26 deaths, two plants, a defuse, a 14 MB demo PUT into the platform's dev MinIO — with 138 link frames, 53 node frames and 9 MatchZy payloads in them (the first `--write-fixtures` attempt wrote them empty: the dev orchestrator's trace is `.cache/trace/dev.ndjson` and `--trace` had been pointed at a file nobody writes; restored and re-run). Fifteen real matches on the dev node this iteration, every one released, `openServersAfter: 0` and the ledger row closed on each. `pnpm verify` green (19/19), `pnpm verify:extended` green (19/19 + 3/3, 410 TS tests, 167 C#), and the `EZPUG_CS2_TESTS=required` lane green on its own in 19 minutes with the tightened assertion; the Dathost lane skipped with its printed reason. **One P1 raised, T21b:** `pnpm dev:node up` starts a second agent on top of the first and the two fight over the node socket forever — it cost two runs here (one with `fleet.node_disconnected` × 1910, the next a `503 no_capable_server` against an idle node) and `dev-node.sh status` reads healthy throughout. Pre-existing, not caused by this task, written into the PRD rather than fixed inside it. — 931b573 — 2026-09-06
T21b: one agent per node identity, and a flap that costs one fact instead of one per close. Three fixes, one per half of the finding. **The script** (`scripts/dev-node.sh`) stops trusting `.ezpug-node/agent.pid` — it held the outermost `pnpm` of three, so `down` killed a wrapper and left the agent behind, and the next `up` found a dead pid and started a second one on top of a live one. Discovery is now by what is running: `pgrep -f 'src/main.ts run'` narrowed to processes whose **cwd is `apps/node`**, which matters more than it looks — `apps/orchestrator` runs a `src/main.ts` of its own and `/root/ezpug` runs two more, and an unscoped matcher would have had `down` kill the dev orchestrator. `up` adopts a live agent (and stops all of them first if it ever finds more than one), `down` ends every agent this checkout owns **by process group** (`setsid` on start, so one signal takes `pnpm` → `pnpm` → `tsx` → node together), `status` says in red when the answer is not one. Counting is by distinct pgid and is a floor rather than a census — an agent started by hand through `npx` brings a launcher shell of its own — so no message ever prints the number, only whether it is more than one. Two bash traps paid for on the way: `agent_pids | head -1` gives the loop a SIGPIPE that `set -o pipefail` turns into a **silent** exit of the whole script (it swallowed an `up` completely before it was found), and `$$`'s own process group must never be signalled because an older `up` shared one with the script. **The link** (`link/node-link.ts`) stops letting a losing socket speak for the identity: the replaced socket is closed **after** the new session owns the id rather than before an `await`, and a socket that is no longer the current session writes nothing on its way out — the old order let the outgoing close mark the row `connected: false` and detach the registry entry the incoming hello was about to fill. A hello that replaces a session younger than one heartbeat is logged as the warning it is, which is the only thing that made this visible from either side. **The provider** (`providers/nodes/provider.ts`) debounces the fact: a close arms a 15 s grace timer (`NODE_DISCONNECT_GRACE_MS`, an order of magnitude above the agent's first reconnect backoff and well inside `NODE_LOST_MS`), only silence for the whole window is an incident, and a node must hold the wire for the same window before the next absence can raise a second one — so a flapping identity costs exactly one `fleet.node_disconnected`, not 1910. Proved live on this box, which was **already carrying an orphan** from a T21a run when the iteration opened: the new `status` found it, `down` ended it with its wrappers, and then a deliberately hand-started second agent reproduced the fight (1011 ms flap cycle, 100 warn lines in 100 seconds) while `GET /v1/capacity` kept answering `available: 2` throughout — the `503 no_capable_server` half of the finding — and `up` detected it, stopped both and left one. Four orchestrator tests (the grace, the second incident, the two-agent flap, and the existing `fleet.node_disconnected` one taught to advance the clock); each new one was checked against the old code and fails there. `docs/nodes.md` carries the rule and the reason. `pnpm verify` green (19/19, 413 TS + 167 C#), `pnpm verify:extended` green (19/19 + 3/3 + the release check), and the `EZPUG_CS2_TESTS=required` lane green on its own in 17.5 minutes — a whole real `pug` on the dev node, demo landed, container gone, zero open ledger rows — with the Dathost lane skipped for its printed reason. **One P1 raised, T21c:** the *first* `pnpm verify` of this iteration failed `conformance.extended.test.ts`'s `happy-bo1` on `the match failed before it was ready`; it then passed 1 of 1 alone and **3 of 3 under six spin loops**, so T10a's synthetic-load posture does not reproduce it and the whole verify is the reproduction. Pre-existing — that file pins `EZPUG_IRON_PROVIDERS: 'sim'`, so nothing this task changed is registered in it — and untraced further because the tier is on the system clock and the flow prints the state without the reason, while `afterAll` sweeps the row that would carry it. Written into the PRD rather than fixed inside this task, and never retried into green. — 342966d — 2026-09-06
T21c: the P1 was not a deadline a starved process blows — it was **a neighbour orchestrator acting on rows it does not own**, and a `pnpm verify` is exactly when a neighbour exists. Traced by standing two orchestrators on the one test database and reconciling: `provider_error: server lost before going live: sim no longer lists server sim-1` — the very words behind `the match failed before it was ready` — with no load, no starvation and no fake clock involved. The mechanism generalises past the reaper: **the three things a process does on its own initiative all read the whole database and act on what they find.** The reaper (`providers/reaper.ts`) held every open ledger row against *its own* providers, so a neighbour's row is a server nothing lists → `matches.suspect` → `status()` answers `gone` for an id it never minted → the match fails. The boot's `resume()` (`match/machine.ts:1479`) re-armed every open match and re-ran the walk for any still `pending`/`allocating` — two machines on one row and a second server for a match that already has one; it fires at a neighbour suite's `orchestrator.start()`, which under a whole verify is scheduled anywhere in the 330 s `conformance.extended.test.ts` is running (alone it boots before the first match exists, which is why the file was green alone and green under T10a's spin loops). The webhook worker's `listDueDeliveries` would POST a neighbour's envelopes and race it for the row. **The fix is one identity, both halves:** `EZPUG_IRON_DEPLOYMENT` (`src/deployment.ts`, default `ezpug`) is stamped on every match and every ledger row by the *store* — `MatchInsert`/`ServerInsert` omit the field so no caller can get it wrong — and `listOpenMatches`, `listOpenServers` and `listDueDeliveries` (an inner join to its match) answer only for it; it is also the Dathost `user_data` tag, which had the same rule on the provider side since T16 and no wire to the ledger's. Read-only history is deliberately *not* narrowed: `listLedger` and a month's spend belong to the key, not to the process. One additive migration (`0003_deployment-scope`, two columns with the default and two indexes; applied to the dev and test databases, and old code keeps writing through the default). The two DB-backed suites now each run as their own deployment, which is the isolation `db/testing.ts` already asked for and only had for key names. New extended suite `deployments.extended.test.ts` (3 tests): a neighbour's reconcile leaves the match and the fleet alone, a neighbour's boot adopts neither the match nor its deliveries, and the owner still finds its own loss — each of the three filters was reverted in turn and the suite goes red for each. `@ezpug/match-api` **0.4.1** (fixtures only, changelogged): the flows that wait for ready/live now print the `endedReason` beside the state, which is what made the trace readable at all — the row that carries it is swept by `afterAll`. `pnpm verify` green ×3 (19/19, 417 TS + 167 C#), `pnpm verify:extended` green (19/19 + 3/3 + the 0.4.1 publish check), and the `EZPUG_CS2_TESTS=required` lane green in 7.6 minutes on the restarted dev orchestrator — a whole real `pug` on the dev node against the migrated database, demo landed, container gone, every one of the dev ledger's 36 rows closed and stamped `ezpug`. **Honest about the reproduction:** the original one-off was never reproduced on demand (T21b could not either); what is proven is a mechanism that produces its exact words deterministically, in the exact conditions a whole verify creates. **One P1 raised, T21d:** the *first* `pnpm verify:extended` of this iteration failed `machine.test.ts`'s `ends ttl_expired …` with `expected 'completed' to be 'ttl_expired'` — a fake-clock test losing to the sim's own ending; pre-existing (the three reads this task narrowed are tautologies for a single in-memory store) and untraced, with the two suspects written into the PRD: T10a's un-awaited sim ingest, and a sim story seeded on `${root}#${randomUUID()}` so its length is never the same twice. It passed 8 of 8 alone afterwards and the re-run of the extended tier was green; never retried into green, and this line says which runs were which. — f1ffaee — 2026-09-07
T21d: the ttl test was a coin toss on the story's length, not a barrier escape. Measured before touching anything: 500 matches through the exact test shape, each with a fresh `randomUUID()` match id feeding the seed `<root>#<matchId>`, gave a `series_end` anywhere from **583 796 ms to 1 256 840 ms** of fake time against a ttl armed at **600 000** — the whole assertion sat inside a 3 % window of a distribution that is 2x wide. The failure rate is about **4 in 3 000** (2/500 loaded, 0/500 loaded, 0/500 idle, 2/1 500 loaded), which is why a full verify found it once and eight runs alone did not; the load correlation the finding suspected is noise, the seed lottery is the whole mechanism. Two LOST timelines printed it plainly: `series_end@596692 … match.ended@600000 {"kind":"completed"}` and `series_end@583796 … match.ended@600000`. **The barrier is not at fault and I checked rather than assumed**: 200 matches played through `playOut()` (one timer, then settle) gave a `series_end` -> `match.ended` fake-time drift of **0 ms, 200 times out of 200**, so `settle()`'s promise holds; the 3 308 ms smear in the LOST runs is `clock.advance(600_000)` firing a whole window of timers with only `flushMicrotasks()` between them, which is what a bulk advance is *for* and not something `settle()` ever claimed to cover — T10a's `sim.pending()` counting is intact and `providers/sim/channel.ts:51` is not in this path. The fix is therefore the margin, made structural instead of lucky, and it is one test file: the request pins `sim.seed` so the story is the same story every run (the `randomUUID()` no longer reaches the prng at all), and asks for `regulationRounds: 24` so the story **cannot** finish inside the window — `going_live` at 168 344, `series_end` at 6 916 360, a ttl at 600 000 sitting 11.5x inside it rather than 1.03x outside it. A `live` read at 600 000 - 1 is the margin's guard: if a future story ever shortens, that line fails naming the reason instead of the last line failing on a coin toss. The test's own words ("whatever the server says") are now true — the server is mid-round when the lifetime runs out. Proof: the whole `machine.test.ts` **10 of 10 green with 14 busy CPU loops beside it**, a real `pnpm verify` green with the orchestrator suite executed uncached (417 passed, 4 skipped; `conformance.extended.test.ts` 12 green inside it), and a real `pnpm verify:extended` green end to end (exit 0; its inner `pnpm verify` replayed from turbo cache, `turbo run test:extended` and `release.mjs check` executed). Nothing outside the test changed — no production code, no fixture, no contract. — fe5ae56 — 2026-09-07
T22: a mode with no plugin anywhere tells a whole match. `GenericFlow` (`plugins/EZPug.Sdk/Gamemodes/GenericFlow.cs`, owned by the runtime so it is in every server and a `config` mode needs nothing to enable it) reads the flow off the engine for `flow: plugin | none`: `going_live` at the first round start outside warmup, `round_start`/`round_end` with the winner, the win condition and the two team scores as `cs_gamerules` keeps them, `map_end` on the win panel and `series_end` with it when the map that ended was the last one the request planned. **`side_swap` is emitted although T22 does not name it**, because without it the score is wrong: CS2 swaps the team scores along with the players at halftime, so team A's rounds land on team B from there on unless the runtime's sides follow — the halftime flag is transient and polled every 250 ms (MatchZyFlow's posture), and a new map of a series takes the ends its plan named. A mode that knows better says `Gamemode.OwnsFlow` and the emitter falls silent. Two callbacks per round start, not one, because emitting `going_live` resets the round counter by the vocabulary's rule and the runtime numbers the round from the engine immediately after. **It also starts the match, because for these flows nobody else can** — three engine findings paid for in four whole matches on the dev node (CS2 1.41.7.8): (1) `mp_warmuptime` does not run down on a dedicated server — a warmup restarted on `mp_warmuptime 20`, `mp_warmup_pausetimer 0`, both `mp_warmup_online_enabled` and `mp_warmup_offline_enabled` at 1, was still the warmup period 72 s later, and the next `mp_warmup_end` reached freeze time inside a frame (the first run died of it: twenty minutes of warmup and an honest `ttl_expired`); (2) an `mp_warmup_end` in the *cfg*, one second into the map, is undone by the engine re-entering warmup right after it; (3) the engine re-enters warmup under a match that has already gone live, and seventy-four deaths later the gamerules still said no round had been played — so the emitter ends warmup 20 s after the assigned map is up and again every 10 s **for as long as the gamerules say warmup, live or not**, and the delay is what the roster and the bots arrive in. `flying-scoutsman` 0.2.0: `openJoin: true`, a description that no longer claims the mode has no flow, and `cfg/ezpug/flying-scoutsman.cfg` — CS2 ships no `gamemode_scoutsknives.cfg` to copy (`scoutsknives` appears nowhere in `libserver.so`, `pak01_dir.vpk` or the whole `cfg/` set), so the mode is written out cvar by cvar, every name checked against `libserver.so` first: SSG08 + knife, `sv_gravity 200`, no economy, no bomb, `mp_halftime 1` kept deliberately so the swap is exercised. Six SDK tests (a whole map with no mode attached, a two-map series, a matchzy flow that hears nothing, a mode that owns its flow, the warmup ending and its retries); three existing tests grew the flow events a `plugin` flow now legitimately gets. `@ezpug/match-api` **0.4.2** (catalog only, changelogged: the fake serves the manifest, so `config-only` and `happy-bo1` were re-recorded). **The proof**: a real `flying-scoutsman` on the dev node, `going_live` → three rounds each with a winner, a condition and a score → `side_swap` at halftime → `map_end` 0–3 → `series_end` → `match.ended: completed`, one ledger row closed, recorded into `real-flying-scoutsman-{link,node}.json` and `real-flying-scoutsman-bo1.json`. `pnpm verify` green (19/19, 417 TS + 4 skipped, 174 C#) and `pnpm verify:extended` green (19/19 + 3/3 + the 0.4.2 publish check). **The `EZPUG_CS2_TESTS` lane is red and is P1 T22c**, on one assertion and nothing else: a `pug` played perfectly (eight rounds, two side swaps, the demo uploaded, the row closed) but said `server_ready` twice, 1.34 s apart against a 1 s `MapReadyDelayMs` — the boot map's delayed map-start racing the `assign`, in code this task did not touch. **Two more P1s raised:** T22a, the loader's flat cvars land in the same console frame as the mode's cfg so the two net out (measured: `bot_kick; bot_quota 0` in the cfg and `bot_quota 10` in the request gave an empty server for twenty minutes, and one `bot_quota 10` over RCON filled it in a second — worked around in `iron-match.mjs`, which asks for the bots at `ready` for every non-`matchzy` flow, rather than reordering the loader inside this task); and T22b, `pnpm verify` is red while `pnpm cs2:up` is running, because a unit test dials 127.0.0.1:27415 expecting nothing there and the documented dev CS2 lane is exactly there (confirmed both ways). None retried into green.

T22a: the loader gives the mode's cfg a console frame of its own and everything the assignment asks for the next, so a request's `rules.cvars` is a change and not the undoing of one. The seam is the SDK's, not the loader's: `GamemodeRuntime.SettleThen(delayMs, rest)`, callable only from inside `MapLoaded` — it runs `rest` a beat later and holds `server_ready` *and* the mode's `OnStart` until it has, so the hook's promise ("the map is up" means "and configured") survives a host that cannot say everything in one frame; a release, a reassignment or the next map start cancels a beat still pending and nothing is said for a match the server no longer holds. `GamemodeLoader.OnMapLoaded` now execs the cfg and nothing else, and `Configure` — the flat cvars, then the match config, `matchzy_loadmatch`, the remote log and the backup, the pug lane's order untouched — runs `CvarSettleMs` (1 s, the same beat `MapReadyDelayMs` and MatchZy already wait) later. `iron-match.mjs` puts `bot_quota` back into `rules.cvars` for **every** flow and the non-`matchzy` RCON fill at `ready` is gone: a client should not need a fleet door to get the bots it asked for. Proven on the dev node against CS2 1.41.7.8 with the image rebuilt (`pnpm cs2:build`, new digest, agent restarted): `flying-scoutsman` had **ten bots standing from the map alone, no RCON**, and played out — `going_live`, ten rounds, two side swaps, 64 deaths, `map_end`, `series_end`, `match.ended: completed`, one ledger row closed, nothing left running. The `pug` lane under `EZPUG_CS2_TESTS=required` was equally whole — three rounds to a 3–0 clinch, a side swap, `map_end`, `series_end`, `demo_available` + `demo.uploaded`, GOTV alive through MatchZy's `warmup.cfg` and `live.cfg`, the row closed — and **is still red on the one assertion T22c owns**: `server_ready` twice, the boot-map race, pre-existing and untouched here (this task can only ever drop that second one, never add it). Docs true again: `docs/sdk.md` (the hook table and a paragraph on the beat), `docs/gamemodes.md` (`cfg`), `docs/operations.md`, `plugins/README.md`, and the measurement in `flying-scoutsman.cfg` rewritten to say what the loader does now. `pnpm verify` green both languages (19/19, 417 TS + 4 skipped, 179 C#). Never retried into green. — 40ce143 — 2026-09-07

T22b: `pnpm verify` no longer shares a port number with `pnpm cs2:up`. The collision was one line in one rig: `createNodeRig` left `portBase` unset, so the nodes provider used its production default `NODE_PORT_BASE = 27415` — the same number `.env.example` gives the dev CS2 lane — and `is an honest 503 with an audit line when the container is not listening` opens a **real** socket to that game port. With a container up the dial reached a server that answers and the audit read `<failed: auth_failed>`, not `<failed: unreachable>`. **The rig now owns its ports instead of borrowing the provider's default:** one probe per file walks windows of eight from `RIG_PORT_BASE = 27600` up, binding each candidate to prove nobody holds it, and every rig gets the first free window; `rig.portBase` is on the `NodeRig` interface so the three assertions that pinned `27415`/`27416` literally now read the rig's own pair rather than a number. The base sits **above both documented lanes and below the kernel's ephemeral range** (`/proc/sys/net/ipv4/ip_local_port_range` is 32768-60999 on this box), which is the half that makes the probe safe: nothing can be handed the port between the bind test and the dial, so releasing it is not a race. The probe alone would in fact have been enough — with it in place even a `RIG_PORT_BASE` of 27415 walks past an occupied lane to 27423 — but the two are worth having together, one so the number is never shared by construction and one so it is never shared by accident. Proven both ways with a stand-in listener holding 27415: the file is 25/25 green with the port occupied, and forcing `portBase` back to a bare `27415` turns the 503 test red in the same condition (a 20 s timeout against my stand-in; a real CS2 server gives the `auth_failed` T22 recorded — same collision, either symptom). Full `pnpm verify` green with 27415 still held, including the extended conformance tier that happened to be up (41 files, 417 passed, 179 C#). `.env.example` — the one place this repo decides a port — now says out loud that the CS2 pair is a running server's and nothing in verify may dial it, and where the tests' window is instead. — 54d69bd — 2026-09-07
T22c: the boot map is not the match's map. The seam carried only a name, so a MapStarted that fired inside CounterStrikeWorld's one-second ready delay looked identical whether the engine had started it before the assign or after the loader's changelevel — and on a freshly booted container the assign lands inside that second, so the runtime exec'd the cfg on the boot map and said server_ready for a map that was never the match's, a second before the real one. Fixed where the two halves meet, with both halves of the fix: IGameWorld.MapStarted now carries a MapStart(Map, StartedAtMs) whose instant is stamped in CounterStrikeWorld's OnMapStart listener rather than when the delayed callback fires, and GamemodeLoader.OnAssigned calls the new GamemodeRuntime.ExpectMapChange() before changelevel/host_workshop_map so every map the engine began before that call is dropped with a log line and the first one that began after it is the match's. Chose the timestamp over a bare token because a token cannot tell "the map I asked for" from "a map that started while I was asking"; ExpectMapChange is cleared on assign and on release, and MatchZy's own next map of a series still passes through untouched (nobody asked for it). CorePlugin's booting->idle hook and FakeGameWorld.StartMap(map, startedAtMs?) follow the seam; a new GamemodeLoaderTests case writes the race deterministically (assign at 500 ms, boot map announced with startedAtMs 0) and asserts one server_ready, for de_mirage, with no exec on de_dust2. Documented in docs/sdk.md beside SettleThen and in plugins/README.md. pnpm verify green (417 passed, 4 skipped; 49 + 129 C#); re-proven with EZPUG_CS2_TESTS=required on the rebuilt image — the pug lane's 4 tests passed in 489 s, the strict "expected 2 to be 1" assertion untouched and the ledger row closed, on the first and only run of the lane. real-pug-bo1.json already held one server_ready, so the recorded shape did not change. — fe64a74 — 2026-09-07
T23: retakes plays on real hardware. **cs2-retakes 3.1.0** is source under `plugins/vendor/cs2-retakes/` — upstream's tree verbatim minus `.git`, GPL-3.0 licence kept, shielded from `plugins/Directory.Build.props` by an empty `plugins/vendor/Directory.Build.props` (MSBuild stops at the first one it walks into) so it builds with upstream's own properties and CSS 1.0.369, out of `plugins/EZPug.sln` so `pnpm verify` never compiles somebody else's warnings. `plugins/vendor/build.sh` builds it into `plugins/disabled/RetakesPlugin/` **with its `map_config/` spawn set** (the csproj copies `lang/` and not the spawns — upstream ships those from the release zip, and without them a retakes server has no map it can play) and puts `RetakesPluginShared` once, in `shared/`. **The allocator is a release binary, not source** (`Ravid-A/cs2-retakes-weapon-allocator` v3.2.5, `docker/cs2/Dockerfile` + SHA-256, MatchZy's pattern): it is the only allocator built against what we actually ship — `net10.0`, CSS **1.0.373** exactly, RetakesPluginShared 2.0.0 — but **its repository carries no licence file**, and its ancestor being GPL-3.0 is not a licence to copy somebody's source into a public repo; its zip is upstream's own build of the same tree, laid out exactly like the folder it extracts into. Vendored *source* pins now have a machine-readable home (`plugins/vendor/vendored.json`: repo, tag, commit, and the line of source carrying the version) that `scripts/check-pins.mjs` holds against `docs/pins.md`; the binary ones are a version and a checksum in the image. **The new door: `assign.pluginConfigs`** (`packages/protocol`, C# regenerated in the same commit — no `@ezpug/match-api` change needed, and none wanted: the file belongs to the *plugin*, not the mode). A community plugin whose settings are not cvars gets one document per plugin folder, and the loader writes each to `addons/counterstrikesharp/configs/plugins/<folder>/<folder>.json` **before the first `css_plugins load`** — CounterStrikeSharp reads it once, inside `InitializeConfig`, on the way into `Load`, so a file a frame later is a file nobody opens (`ConfigManager.Load` keyed on `Path.GetFileName(ModuleDirectory)`, read out of CSS v1.0.373). A key that is not a plain folder name is refused; a `<folder>.toml` beside it wins inside CSS and is warned about rather than deleted; `release` deletes what it wrote. `match-config/retakes.ts` builds it from the manifest and is **deliberately partial** — four keys, everything else keeps the vendor's default: `MaxPlayers` from `slots.teamSize × teams` clamped to the plugin's 2–10, `ShouldAutoJoinGame` from `slots.openJoin`, `EnableFallbackAllocation: false` while the allocator is enabled (and back to `true` when the image lacks it), and both `Queue*Flag` arrays emptied because upstream defaults them to `@css/vip` and this round has one permission mechanism. `retakes` manifest **0.2.0**: both plugins in load order (the allocator resolves the retakes capability 0.1 s after its own `Load`), `de_train` added so the catalog is exactly the maps the pinned release ships spawns for, and `cvars` carrying only `bot_quota_mode`/`bot_join_after_player` — because **retakes execs its own `cfg/cs2-retakes/retakes.cfg` one second after map start**, the same second the loader execs the mode's cfg, so anything that must beat it belongs in the manifest (a frame later by construction, `CvarSettleMs`) and not in `ezpug/retakes.cfg`, which is therefore six lines of server hygiene and a long comment saying why. `@ezpug/match-api` **0.4.3** (catalog only, changelogged). **The proof**: a real `retakes` on the dev node against the image that ships — retakes' own log showing 5T vs 5CT, per-map spawns, auto-plant and bombsite announcements — `going_live` → six rounds each with a winner and a condition → `map_end` → `series_end` → `match.ended: completed`, 59 deaths, one `server_ready`, one ledger row closed, no server left running, recorded into `real-retakes-{link,node}.json` (212 link frames, the `assign` carrying `pluginConfigs`) and `real-retakes-bo1.json`. **One P0 of my own caught before it shipped a lie**: the first recording wrote an *empty* `LinkExchange` because the orchestrator resolves `EZPUG_IRON_TRACE_FILE` against its own package directory and `iron-match` against the repo root — the script now looks in both, takes an absolute path as given, and **dies** rather than recording a match nobody watched (`docs/operations.md` says so). `pnpm verify` green (19/19, 424 TS + 4 skipped, 180 C#), `pnpm verify:extended` green, and the `EZPUG_CS2_TESTS=required` pug lane green 4/4 (demo uploaded, one `server_ready` — T22c holds). Not visual: nobody looked at the game. Upstream's allocator throws once per disconnect at load (`Votes_OnPlayerDisconnect`, `player.UserId!.Value` on a controller with none); CSS catches it, the match is unaffected, and it is not ours to patch — written down in `plugins/vendor/README.md`.

T24: player commands, player tokens and the widget socket. **The contract first** (`@ezpug/match-api` **0.5.0**, additive, changelogged): `GET /v1/widget` is the second upgrade in the route table and `widget/socket.ts` its frames — up `hello { protocol: 1, token }` (the token in the first frame, **never in the URL**, so no request log and no browser history ever holds one) and `command { correlationId, command, args? }`; down `hello { matchId, steamId64, gamemode, state, locale?, commands: WidgetCommandState[] }` (the manifest's specs plus `chargesLeft`/`readyInMs` as last relayed — a hint for the buttons, the SDK is the truth), `event { envelope }` (every durable fact, so a widget sees its own tap land and its charges refill without a second socket; position ticks never cross it) and `command_result`. `WIDGET_COMMAND_REFUSALS` = the SDK's seven + `rate_limited`/`not_live`/`unavailable` (a protocol test pins the prefix), `WIDGET_CLOSE_CODES` mirrors the stream's, `WIDGET_COMMAND_RATE_LIMIT` (10 taps, 2/s per token) and `WIDGET_HELLO_TIMEOUT_MS` published. `validatePlayerCommandArgs` is the C# `ArgsValidator` in TypeScript, sentence for sentence. **The sim twin**: `MatchAssignment` carries the manifest's `commands` and `openJoin` (and a player's `locale`), `sim/commands.ts` is the SDK's `CommandTable` in TypeScript (refusal order, cooldowns, charges per life/round/map/match refilled on `player_death`/`round_start`/`going_live`, DE/EN lines), `SimulatedServer.playerCommand` answers like a plugin's `player_command_result` and deals a `player_command` `plugin_event` through the engine's own seq — which is why the recorded `player-command` flow shifted by one seq and nothing else. A stranger on an open-join mode is taken for a joined player (a sim cannot see a human join; documented); `not_alive` is the one check the twin cannot make. **The fake enforces the manifest now**: `fake.playerCommand()` runs through the twin and maps refusals to `ApiError` codes with `details.code`; `fake.widget()` is the socket in-process and `fake.listen()` performs `/v1/widget`. **The orchestrator**: `matches.mintPlayerToken` (rostered, joined = in presence, or open join; `invalid_state` after the end; hashed into `player_tokens`, `ezip_` prefix, 15 min default / 1 h max), `matches.playerCommand` (refuses `not_live`/`unknown_command` without a server, relays to the channel with the widget's own `correlationId`, `unavailable` past the 15 s deadline, **never holds the match's chain** because a real plugin reports the tap's `plugin_event` before it answers the tap), `widget/service.ts` (token → hello → hub subscription forwarding `event` frames and turning the match's close into `4000`, a rate bucket per token hash, door refusals said in DE/EN from the roster profile, a per-process memory of charges/cooldown for the hello) and `widget/upgrade.ts` (the socket: hello timeout on the clock, protocol/malformed closes, taps that arrive during the hello are queued not dropped, slow-consumer close, drained in the stream's step). The sim channel relays to the engine; the sim provider passes the manifest's verbs into the assignment. **Conformance**: the orchestrator now runs every flow — `player-command` in-process through the service (memory tier) and over the real `/v1/widget` socket (extended tier), the target's tap helper in `widget/testing.ts`; the extended cleanups delete `player_tokens` before `matches`. `widget/upgrade.test.ts` runs the socket against a real `/link` with the protocol's fake server as the plugin: hello with verbs and locale, a tap relayed as `player_command` and answered, the mode's `plugin_event` reaching the widget, the SDK's refusal as said, a second socket's hello reporting what was learned, `unknown_command`/`unavailable`/`rate_limited` in the player's language, every close code, expiry on the next tap and at the hello, `4000` after `match.ended`, and the mint rules. One P0 found while proving it: the fake's first cut read "the next envelope" after the engine's answer, but the engine's delivery is queued *behind* the step that asked — so the tap was applied and the envelope was null; it now takes one more turn of the match's queue and finds the envelope by the event's seq. Docs: `match-api.md` (route, "The widget socket", the fake's door), `operations.md` ("The widget socket"), `gamemodes.md`. No C# changed: T7's SDK already answers `player_command`. `pnpm verify` green (19/19, 432 TS + 2 skipped incl. the extended conformance over Postgres because the dev world was up; 180 C#), `pnpm verify:extended` green; the `EZPUG_CS2_TESTS` lane not run (no dev CS2 container up). Not visual: no widget exists yet (T25); the socket was driven by `ws` and the harness. — aef43ea — 2026-09-07T25: `gamemode-kit` — the widget toolchain, the kit a widget is written on, and the orchestrator serving the result. **The kit** (`gamemode-kit/`, `@ezpug/gamemode-kit`, Turbo tag `kit`): `src/index.ts` is the browser runtime — `defineWidget(Component)` makes `<ezpug-widget>` (Vue `defineCustomElement`, shadow root, the host contract as `orchestrator-url` / `player-token` / `locale` / `match-id` attributes or properties, the design tokens as custom properties on the element), `useWidgetLink()` is T24's socket as a composable (hello with the token in the first frame, the verbs with `chargesLeft`/`readyAt` as last reported, `onEvent`, `send()` resolving to the `command_result` by `correlationId` with a 20 s local `unavailable`, backoff reconnect on a network close and never on a `4xxx`, `4000` = `ended`), `connectToHost()`/`mountWidget()` are the widget's half of the `postMessage` handshake (`ready` re-posted every second until `init`, origin **and** window pinned from the first valid `init`, `tokens` re-applied, `size` from a `ResizeObserver`, `orphan` after ten silent seconds), `WIDGET_TOKEN_FALLBACKS` is the platform's `WIDGET_TOKEN_NAMES` list (26 names, pinned equal by a test) with Signal's values so `var(--ui-…)` never resolves to nothing — set on the element, because `--x: var(--x, fb)` is a cycle — and `createT`/`KIT_COPY` are `{ de, en }` copy the manifest's way. Every deadline is on a `WidgetClock` (`browserClock`, the kit's one wall-clock reading, two `biome-ignore`s with reasons; a `FakeClock` satisfies it structurally). The socket/host constants are **mirrored** from `@ezpug/match-api` and pinned by `protocol.test.ts`, because importing one constant from the package's entry would put every Zod schema of the Match API on a phone. **The preset** (`./vite`, `ezpug-widget build`): Vite 8 library mode, one ES module, `codeSplitting: false`, Vue's custom-element mode so an SFC's `<style>` lands in the shadow root, production Vue; `buildWidget` refuses a bundle that contains `fetch(`/`XMLHttpRequest`/`EventSource`/`sendBeacon`/`importScripts`/dynamic `import(` — the frame's CSP has no way out but the socket, so a widget that would break on the match page does not build. `@ezpug/gamemodes` now has a `build` (`ezpug-widget build`, output `*/dist/**`), a `typecheck` (`vue-tsc`) and devDependencies on the kit, Vue and vue-tsc; **the cycle**: kit → match-api (types, the fake for the harness) → gamemodes → kit is broken the way `packages/sim` does it — match-api is a *peer* of the kit, bundled from source by tsdown into `dist/cli.mjs` (Vite, plugin-vue, Hono's node server, `ws`, `hono`, `zod`, `vue` external), `implicitDependencies` for boundaries; the bin is a committed shim (`ezpug-widget.mjs`) so `pnpm install` can link it before a build. Vue `^3.5.41`, Vite `^8.2.2`, plugin-vue `^6.0.8`, vue-tsc `^3.3.11`, happy-dom `^20.11.6` in the catalog (the platform's versions), rows in `docs/pins.md`, checked by `check-pins`. **The harness** (`ezpug-widget dev powerup-dm`, port 3432 decided in `.env.example`): Vite serving `harness/index.html` (the platform's host with switches: locale, watching only, tokens on/off, new match, a log of every message) around a sandboxed `iframe` on `frame.html` (the document as the orchestrator serves it), against the published fake orchestrator in-process on the wall clock with a simulated `powerup-dm` and a token minted for a rostered player; the session is read over `/__harness/session`, never a URL. **Three things the sandbox taught, all fixed at the orchestrator too**: a sandboxed frame's origin is opaque, so (1) its module script is a CORS request from `null` — Vite gets `cors: { origin: '*' }` and the orchestrator's script routes send `Access-Control-Allow-Origin: *`; (2) its socket carries `Origin: null` — **T24's widget door refused it `4005` against `streamAllowedOrigins`**, which would have refused the platform's real mount the day a widget existed; `widget/service.ts` now greets the literal `null` (the token is the credential; a test on the socket pins it); (3) the CSP's `'self'` matches nothing, so `widgetCsp` names hosts explicitly. **The orchestrator** (`widget/bundles.ts`): reads every widget mode's `entry` from `EZPUG_IRON_GAMEMODES_DIR` (default: the workspace's `gamemodes/` through `import.meta.resolve('@ezpug/gamemodes/package.json')`) once at boot, hashes it, logs a line per bundle, warns per missing one; `app.ts` serves `/gamemodes/:id/widget/:hash/index.html` (the document: one module script `./widget.js`, no inline script, CSP `default-src 'none'; script-src <hosts>; connect-src <hosts> ws:// wss://; style-src 'unsafe-inline'`, the hosts being the request's — `x-forwarded-host` behind Traefik — and `EZPUG_IRON_BASE_URL`'s), `/gamemodes/:id/widget/:hash/widget.js` (both a year immutable, a hash not this boot's is `404`) and `/gamemodes/:id/widget.js` (`no-cache`, `ETag`, `304`); the catalog is `decorate(SHIPPED_GAMEMODES)`. **`@ezpug/match-api` 0.6.0** (additive, changelogged, `release.mjs check` green): `widget.url` optional on the served manifest — absent when authored, absent from the fake's catalog (the platform's twin stands in until its pin moves; the platform's `documentUrl` reads what the catalog advertises), absent when the bundle is missing; `powerup-dm` manifest `0.1.1` with `entry: dist/widget.js`; the two recorded goldens that embed the manifest re-recorded (four lines: entry and version, nothing else). `gamemodes.test.ts` skips `node_modules` and `.turbo` in the directory list. **The image**: the Dockerfile collects `gamemodes/*/dist` into `/app/gamemodes` and sets `EZPUG_IRON_GAMEMODES_DIR`; its workspace build now goes through **Turbo** (`pnpm exec turbo run build --filter=@ezpug/orchestrator...`) because `pnpm -r` counts the peer edge as a cycle and started the widget build before the kit existed — proven by building the image: `/app/gamemodes/powerup-dm/dist/widget.js` with the same sha256 as the local build (`cd706ba7…`, deterministic). `pnpm dev` now `dependsOn: ["^build"]` so a dev orchestrator has bundles to serve. **The first widget**: `gamemodes/powerup-dm/widget/` is the kit's generic board (every declared verb as a button with charges and cooldown as last reported, the answer in the player's language) — the pipeline's proof, and T26's starting point (three power-ups, cooldown rings, the peek). **Visual**: driven in headless Chromium (Playwright from the platform's install, `--no-sandbox`; the MCP browser cannot run as root) at **1440 and 390**: the frame mounts, `ready → init → size 168/200`, the widget says `Match läuft`, a tap answers `Angekommen.` with `0 übrig`, the locale switch re-renders `Match live · 0 left · ready · Landed.`, the tokens land on the element, **zero console warnings or errors** (Vite's two debug lines only); screenshots looked at. Tests: kit 19 (link against the fake over a real socket incl. the German `no_charges` line, `4000` → `ended`, `4001` → `refused`, backoff on a network close; handshake; element and shell under happy-dom; the preset over a fixture with the no-fetch check), orchestrator +9 (bundles, routes, headers, CSP, catalog url, 404s, the `null` origin). `pnpm verify` green (24/24: 19 kit, 55 core, 86 sim, 42 protocol, 61 node, 321 match-api, 440 orchestrator + 2 skipped; 51 + 130 C#), the extended tier green (sim 3, match-api 29, orchestrator 440 over Postgres/Redis), `release.mjs check` green. One false alarm on the way: five `conformance.extended` failures that were my own two concurrent runs colliding on seeded key names — green alone and in verify. The `EZPUG_CS2_TESTS` lane not run (no dev CS2 container up; nothing in the plugin changed). Docs: `gamemodes.md` (`url`, the document and its CSP, the sandbox's `null` origin, the token table with fallbacks, "Building a widget", the harness), `match-api.md`, `operations.md` ("The widget bundles"), `README.md`, `gamemode-kit/README.md`, `.env.example`, `pins.md`. — ba08390 — 2026-09-07
T26: `powerup-dm` — the original mode, on the SDK, with a push. **The mode** (`plugins/EZPug.PowerupDm/`, in `EZPug.sln`, published by `publish.sh` into `plugins/disabled/` and carried by the CS2 image): free-for-all deathmatch by cfg, one power-up per life claimed from the phone or with `!powerup <kind>` in chat, three kinds the *manifest* decides (`speed` 1.35× until you die, `armor` a full vest, `radar_peek`), a chat line and a centre card in the player's language, `powerup_claimed` as a `plugin_event`, and its own DE/EN resx pair. One verb with a `kind` enum, not three verbs, so **the SDK enforces "one per life"** (`charges { count: 1, per: life }`) rather than the mode refusing the second and third itself. **The peek is a new kind of frame.** Positions must never be stored (CLAUDE.md), so the five seconds of enemy dots could not be a `plugin_event` and could not be the stream: `@ezpug/match-api` **0.7.0** adds `WidgetPushFrame` (`{ type: 'push', name, data }`, a fourth member of `WidgetServerFrame`, `WIDGET_PUSH_DATA_MAX` 16 KiB) — mode-defined, addressed to the one SteamID64 the token was minted for, never logged, never in `match_events`, never replayed; `packages/protocol` grows the link's `widget_push` frame (matchId + steamId64 + the Match API's push, regenerated into C#, a new `server.json` fixture entry); the SDK grows `IPlatformLink.PushWidget` and `Gamemode.PushWidget(player, name, data)` (dropped, never buffered, because a push that arrives after a reconnect is a lie drawn on a phone); the orchestrator's `server-link.ts` relays it to `widgets.push`, which keeps a `matchId#steamId64` map of open phones beside its per-match one and delivers to that player and nobody else; the kit grows `link.onPush`. Also new, and general: `ArgsValidator.FromChat` — the words after `!verb` become the args schema's **first declared property**, whole, typed, so the chat door and the widget door hand the mode the same object (`!powerup radar_peek` = `{ "kind": "radar_peek" }`). **The widget** (three buttons read off the verb's own `kind` enum — a fourth power-up is a manifest edit — each with a charge/cooldown ring, and an SVG radar centred on the player: contacts clamped to a 2000-unit rim and dimmed beyond it, hollow when more than 180 units above or below, an amber countdown sweep, gone by itself when the last frame expires). The harness fires pushes by hand — a mode may ship `widget/harness-pushes.json` and the page grows a button per entry that calls the fake's new `widgetPush(matchId, steamId64, push)` — because the simulated server has no mode of its own to decide when a push is due. **Real hardware, three runs on the dev node** (`pnpm iron:match --gamemode powerup-dm`, `nodes` provider, ledger closed and no container left each time): run 1 proved the image loads and attaches `EZPug.PowerupDm`, execs `ezpug/powerup-dm.cfg` and plays — 8 bots, **1180 deaths** on de_mirage — and found a real bug: `mp_ignore_round_win_conditions 1` also suppresses the engine's `mp_timelimit` check, so the map could never end and the client had to force it at twelve minutes with one `round_start` and no `round_end`. The cfg dropped that line and made `mp_roundtime` = `mp_timelimit` = 10 (Valve's own deathmatch shape); run 2 ended itself — `round_end`, `map_end`, `series_end`, 880 deaths — and run 3 (two minutes) re-proved the load after the peek was refactored to **one timer for the whole mode** instead of one per claim, since the runtime holds every timer until the match ends. **Visual, and what was not**: the widget driven in headless Chromium at **1440 and 390** against the fake — three buttons, a tap answered `Angekommen.`/`Landed.`, all three spent for the life, the peek pushed and drawn with four dots (one hollow on another floor, one dimmed on the rim), the countdown running down and the canvas removing itself at five seconds, both languages, **zero console warnings or errors**; screenshots looked at. **The tap and the peek were not exercised by a human on a real server** — a bot has no SteamID64 to mint a token for — so that half is proved by the SDK harness under xunit, by the kit against the fake over a real socket, and by `widget/upgrade.test.ts` over a real `/link` and a real `/v1/widget`. Two things fixed on the way that were not mine: `turbo boundaries` was already red on `main` (T25's `@ezpug/gamemodes` → `@ezpug/gamemode-kit` devDependency: turbo walks tags **transitively**, so `data` reached `core` and everything that reads a manifest reached the `kit`) — the two tag lists now say what turbo can actually enforce, with the reason; and a cancelled `pnpm verify` leaves `deployments.extended.test.ts`'s API keys in the test database, which fails every later run on a `conflict` (rows deleted by hand, **T26a** written into the PRD). `pnpm verify` green (24/24; 441 TS + 2 skipped, 186 C# across three assemblies), `pnpm verify:extended` green twice; the `EZPUG_CS2_TESTS` lane not run as a lane — the three matches above were driven by `pnpm iron:match`, which is what it shells out to. Docs: `sdk.md` (the sample mode, `PushWidget`, the chat-args rule), `gamemodes.md` (`onPush`, the harness pushes, the chat form of `args`), `match-api.md` (the `push` frame), `operations.md`, `plugins/README.md`, `gamemode-kit/README.md`, `CHANGELOG.md`. — 1886f62 — 2026-09-07T27: EZ Rating on the scoreboard, and the connect line. The seam grew two members — `IGameWorld.SetScoreboardRating(player, int?)` and `IGamePlayer.ScoreboardRating` (the engine's own value read back, not what was asked for) — over `m_iCompetitiveRanking` + `m_iCompetitiveRankType` at Premier's type `11`, the one rank type whose scoreboard cell is a plain number (decision 21). `EZPug.Sdk`'s new `RatingBoard` owns both halves and hangs off the manifest's `scoreboardRating`, so a mode that did not ask for it gets neither the number nor the line: the roster's rating is drawn on assign, on connect, on a `profile` push, when the map is ready and at every round start (the engine raises `round_start` *after* the round's spawns, which is why that is the redraw a spawn needs too), and cleared on release; a player nobody has a profile for is left alone rather than shown a zero. The **connect line** is bilingual (`rating.connect`, `.rank`, `.unrated`; the number plain, the way the cell writes it) and said **once per connection** — on connect for a rostered player, on their `profile` for somebody who joined open and was a stranger until it arrived; bots are drawn and never talked to. `hello` now claims `scoreboardRating`, which it had been deliberately withholding until this task made it true. **Five real matches on the dev node** (`pnpm iron:match --gamemode retakes`, `nodes` provider, ledger closed and no container left each time) and every one of them found something. Run 1: **`FollowCS2ServerGuidelines`** — CounterStrikeSharp refuses to read *or write* those two fields while it is on, and the throw ate the `command_result` the orchestrator was waiting for, so eight of ten profile pushes came back `provider_unavailable`. The image now writes `core.json` from the pinned release's own `core.example.json` with that one key flipped (a `grep` in the same RUN makes an upstream rename a failed build), the risk is written down in `docs/operations.md` — a GSLT the guidelines could withhold, against a private league's own number on a scoreboard nobody mistakes for matchmaking — and the world catches a refusal, warns once and draws nothing rather than dropping a link command. Run 2: **`ezpug_status` over RCON comes back empty** on a node — a `SERVER_ONLY` CounterStrikeSharp command answers on the *server console*, not to whoever ran it — so the report also goes into the plugin's console buffer and `GET /v1/fleet/servers/:id/console` is the door that hands it back; `iron-match` grew the step that proves all of this without a human (a bot has no Steam account but it does have a `BotIdentity` SteamID64, so the platform can push a `profile` for one exactly as it would for a person who joined open) and the run summary carries the `scoreboard:` line. Runs 3–5 chased the numbers themselves and ended in an honest negative: **a bot keeps the rank *type* and reads its ranking back as `0` however often it is written** — an immediate spawn redraw and a 500 ms deferred one changed nothing, so both were reverted rather than shipped on a falsified measurement. What the hardware therefore proves is the whole path (profile push → link → plugin → controller, read back off the controller through the console) and the *type* on eight players; what it does not prove is the number a human's scoreboard shows, and that is written as a note under T36 where a human will be sitting in front of one. No streak: a roster entry carries none, so the line names the rating and the rank (a note under T27 hands the additive field to T38a). Tests: `RatingBoardTests` (eight, incl. the open-join push, the release clearing, the bot), `ConsoleCommandTests` (the `scoreboard:` line, the hello's new capability), `cs2-image.test.ts` (the flag and its documented risk). Docs: `sdk.md` (a section, both engine facts), `gamemodes.md`, `operations.md`. `pnpm verify` green (24/24; 442 TS + 2 skipped, 194 C#), `pnpm verify:extended` green — CS2 lane and Dathost smoke skipped with their printed reasons, the five real matches driven by `pnpm iron:match`, which is what that lane shells out to. — 28b11ff — 2026-09-07
T28: Skins over the link — the WeaponPaints data-layer fork. cs2-WeaponPaints vendored at `fa8936f3` (tag `build-459`, 3.3a) under `plugins/vendor/WeaponPaints/` as the one patched vendor in the repo; `PATCHES.md` beside it is the whole diff in words plus the table-to-`Loadout` schema, to re-apply on a bump. **The seam**: `EZPug.Sdk.Hosting.ILoadoutSource` (`LoadoutOf(steamId64)`, `LoadoutChanged`) published through CounterStrikeSharp's shared plugin API exactly like `IGamemodeHost` (`LoadoutSource`, `ezpug:loadouts`); the core's `RosterLoadouts` answers out of the assignment's profiles — a lookup, no copy, `null` = default items and never an error — and the runtime grew a `Profiled` host event raised after `Assignment.Push` so what the source reads back is the pushed entry. **The fork**: `WeaponSynchronization` rewritten over the source (synchronous, on the game thread, *replaces* a slot's rows so a refreshed profile that dropped a side drops it), a pure `LoadoutMapper` mirroring the six tables field for field (team 2/3 = t/ct, five sticker slots, the keychain zeros an absent column produced, feature flags gating each table, narrow values clamped never thrown), the writers no-ops, `Database.cs` gone, the menus, MenuManager, `!ws`, `!st`, the GitHub version check and the menu-only config keys gone, `!wp` and `wp_refresh` kept, `!kill` off by default, every connected player re-read on every load (the loader hot-loads it with players standing), and a `profile` push re-reads a connected non-bot player without forcing anything mid-round. Retargeted to net10.0 by importing our `Directory.Build.props` (it references the SDK; upstream compiles clean under `-warnaserror`, no NoWarn needed); in `EZPug.sln` so verify builds and tests it; `build.sh` lays it out like MatchZy's release — dll, `lang/`, the five English catalogue files from upstream's `website/data/` (the 85 MB PHP site is not vendored), its own `Newtonsoft.Json.dll` (the runtime ships none), no `EZPug.Sdk.dll` — and `gamedata/weaponpaints.json` beside the plugins folder. **Two real matches on the dev node** (`pnpm iron:match --gamemode retakes`, `nodes` provider, ledger closed and no container left each time): the loader enabled `WeaponPaints` because the request rostered one loadout (iron-match now does that on open-join non-matchzy runs and pushes a loadout with every bot profile), the fork loaded with no MySQL and found the source, and every one of ten profile pushes produced the core's `skins: profile for … carries a loadout (t: 1 weapon, knife, gloves, agent, music, pin; ct: 1 weapon)` line — now in the run summary as `skins:` — with the fork answering on the same tick. Run 1 found one thing: upstream's gamedata check looks two levels up from the plugin folder, which is wrong for a plugin under `plugins/disabled/`, so every boot logged a false "upload weaponpaints.json" error while CounterStrikeSharp had already loaded the file — fixed (`CounterStrikeSharpRoot()` finds the folder by name), image rebuilt, run 2 clean. **What is not proven**: a bot keeps default items by upstream's own `IsBot` checks on every apply path, which this repo does not patch, so nothing was applied or read back through the world and nothing was visual; the pixel is a human with a client under T36 (note under T28). The allocator's known disconnect throw appeared twice as before. Tests: `WeaponPaints.Tests` (six, the mapper against the recorded assign frame), `RosterLoadoutsTests` (five), the `Profiled` and `LoadoutSource` capability tests in `HostHooksTests`, `cs2-image.test.ts` refusing MySQL, Dapper or MenuManager in the fork's code and holding `build.sh` to the layout. Docs: `pins.md` (row, the net10.0 paragraph), `plugins/vendor/README.md`, `plugins/README.md`, `operations.md` (the guidelines flag now covers skins too), `sdk.md` (the seam, the `Profiled` row), `gamemodes.md`, the Dockerfile's comments, `check-pins` (27 pins). `pnpm verify` green (24/24; 443 TS + 2 skipped, 210 C# across four assemblies), `pnpm lint` green. — a0b368a — 2026-09-07
T29: branding and the connect card. **One voice, four places, all of it off the assignment.** The SDK grew `Branding` (`Runtime.Brand`) and `ChatColor`: the hostname, the chat prefix, the two team names and the centre card, with nothing per-server anywhere. **The hostname has one home now** — `Branding.HostnameFor` (and `PrettyMap` with it), the loader delegates and still writes the same string into `matchzy_hostname_format`: the request's when it named one, else `EZPug · <event> · <mode> · <Map>` with the event only where `branding.eventName` was given, clamped at 63 rather than left for the browser to cut. **The prefix goes on every line the SDK says** — the rating connect line, a refused player command, a mode's `Say`/`SayAll` — and deliberately *not* on a client's `announce` or `ezpug_announce`: relayed words are the platform's to brand, which is the one rule that decided which call sites moved. The palette is CounterStrikeSharp's `ChatColors` **read off the restored 1.0.373 assembly rather than remembered**, copied in as code points so the SDK still holds no game type on this seam; every name from outside (event, team, hostname) goes through `ChatColor.Strip` and the card's markup through an escape, so nobody colours a chat line or breaks a panel by what they called their team. **Team names are painted by the side in force** (CT blue, T gold) and move with `side_swap`, so chat and the scoreboard agree after halftime; a rostered player is told which team they play for on connect, a free-for-all (`slots.teams: 1`) is told none because it has none, an open-join stranger none until a `profile` arrives, a bot nothing. **The card is HTML through `PrintHud`** (`PrintToCenterHtml`, which the engine holds up for about five seconds, so no repeat timer), two seconds after the connect because the connect is `player_connect_full` and a card in that instant lands behind the client's own loading chatter: event-or-EZPug, the manifest's own bilingual title, one instruction chosen by the manifest (`.ready` for a `matchzy` flow, the phone for a mode with commands or a widget, otherwise "have fun"), and `ezpug.com` — an SDK constant, because a platform URL in the branding block would have been a Match API change T29 did not need. **What is not proven**: the pixels. A bot reads neither chat nor a centre panel, so everything here is proved against the world seam and the card waits for a human client (notes under T29 and T36). Tests: `BrandingTests` (twelve, sixteen cases — hostname variants and the clamp, the prefix and a loud event name, the colours across a swap, the card in both languages, per flow, cancelled by a disconnect or a release, escaped, and the players already standing when the match arrives), `FakeGameWorld.Hudded` and `AssignmentFor(branding:)` in the harness, and the rating, harness and powerup suites moved to `Branding.Prefixed(...)` expectations rather than bare strings. Docs: `sdk.md` (a Branding section with the four places), `gamemodes.md` (the protected `hostname` row). `pnpm verify` green (24/24; 443 TS + 2 skipped, 221 C# across four assemblies), `pnpm lint` green. — 2b62b82 — 2026-09-07
T30: the chat bridge and the warmup lines. **The bridge was already up** — the runtime has emitted `chat_message`/`chat_command` off the world's `ChatSaid` hook since T22, split once by `ChatParser` and gated on the manifest's `chat` capability, and T29 decided a client's `announce` is relayed unbranded — so this task is what was missing around it. **`WarmupChat` (`Runtime.Warmup`)**: the assignment's `warmupLines` printed one every `IntervalMs` (8 s) in the order the client wrote them, **cycling** so a late arrival reads the whole set, armed at `server_ready` rather than `assign` and silenced at `release`. Warmup is the engine's word — `IGameWorld.Rules.Warmup` where there are gamerules, `!Match.Live` where there are none — so a knife round and a live round are quiet and the next map's warmup speaks again. The lines carry **no prefix**: they are the client's words like an `announce`, rendered by the platform in the roster's majority locale (`Assignment.MajorityLocale()`), and the server does not talk over them. **One sanitizer for every line from outside** — `SaidLine.Sanitize`, the C# twin of the simulator's `sanitizeChatLine` down to its vectors: control characters (the colour palette lives there), `;`, `"` and `\` become spaces, whitespace collapses, 127 code points, and nothing left is `null` — so the link's `announce` (refused `validation_failed`), `ezpug_announce` on the console and a warmup line all print the same line. **The sim says the same**: `MatchAssignment.warmupLines` threaded from the request through both doors (the orchestrator's `sim` provider and the fake), the story merging `chat_announced` `plugin_event`s into the warmup window by time on nobody's dice — a seeded match without lines plays byte-for-byte the match it played before — and `SIM_CHAT_EVENT` moved to `chat.ts` so the story can reach it without a cycle. `@ezpug/match-api` **0.7.1** (no schema moved; the fake finally honours a field it has had since 0.1.0), changelogged. Tests: `WarmupChatTests` (cycling, the warmup edge both ways, a release, a dropped line, announce), two story tests, one in the fake's suite and one in the sim provider's. Docs: `sdk.md` (a warmup-lines section and the sanitizer), `match-api.md`, `operations.md`. `pnpm verify` green (24/24; 444 TS + 2 skipped, 236 C#), `pnpm verify:extended` green (exit 0, conformance + fault injection + the publish check); the `EZPUG_CS2_TESTS` lane skipped — the dev CS2 container is down, and a bot reads no chat anyway. — 5e352a3 — 2026-09-07

T26a: The sweep reads the residue out of the database instead of out of a memory nobody kept. **What the bug actually was.** Three suites commit — `standing.test.ts` (the HTTP path has no transaction to roll back), `conformance.extended.test.ts`, `deployments.extended.test.ts` — and each undid its rows in `afterAll` from a `minted: string[]` the *process* had been pushing key ids onto. `afterAll` is precisely the hook a cancelled run never reaches, and a cancelled run is the ordinary shape of a red `pnpm verify`: Turbo kills the siblings the moment one task fails. So the keys stayed, and because `api_keys` has a unique index on the name of every key that is not revoked, the *next* run failed `conflict: an API key named … already exists` on a change that had nothing to do with it (found in T26; the rows were deleted by hand). **The fix is a change of source.** Every committed key name already carries the file's `testNamespace`, and every other row a suite writes hangs off one of those keys, so the whole of a run's residue is reachable from `api_keys.name like '<namespace>-%'` — a question the *database* answers, which is why it can be asked in `beforeAll`, about a run that is already over. `sweepNamespace(db, namespace)` in `db/testing.ts` is the one helper now (with `sweepTestNamespace(config, namespace)` opening a connection of its own, because a suite's pool is opened after the first sweep and closed before the last), and the three suites call it in `beforeAll` and `afterAll`; their `minted` arrays are gone. **Two things the old copies missed**, found by writing the helper's own test rather than by a failure: `api_key_budget_notices` references `api_keys` (T5) and would have blocked the delete the first time a conformance flow crossed a budget threshold, and a GSLT lease (T17) is a *pointer*, not a reference — nothing stops the server row's delete, and the account sits out of the pool forever — so the sweep nulls `leased_by_server_id` for the rows it takes, and takes any node the swept key enrolled. `db/sweep.test.ts` commits a whole suite's shape (key, webhook secret, budget notice, match, event, delivery, ledger row, server token, leased GSLT, node) under two sibling namespaces, sweeps one, and proves the other untouched; it cleans up with its own subject. Proved end to end by planting a `t-standing-test-…-root` key by hand and watching the standing suite sweep it and pass where it used to conflict. `pnpm verify` green (24/24; 446 TS + 2 skipped, 236 C#), `pnpm verify:extended` green (exit 0), the test database at zero rows after both; the `EZPUG_CS2_TESTS` lane skipped — the dev CS2 container is down and this task never reaches a server. — c2e6364 — 2026-09-07

T31: fleet facts and provider health — the four `fleet.*` facts get a destination and the providers get a heartbeat. **The probe loop** (`apps/orchestrator/src/providers/probes.ts`) asks every provider the cheapest question it has every 30 s on the injected clock, bounded to 5 s, and writes the answer into the registry that `GET /v1/fleet/providers` already served: Dathost `GET /account` (never the template — "Dathost is down" and "somebody deleted the template" are different alarms, and the account read touches nothing), the node provider its heartbeats, a provider with no `probe` verb its `offerings()`, which is what the sim answers in-process and therefore always up. `/healthz` asks the same question live rather than off the offerings cache, so credentials that stopped working are not healthy. **One fact per incident**: the first failing pass says `fleet.provider_unreachable` into every open match with a row on that provider, a pass that finds it still down says nothing, a success closes the incident and the next outage is a new one — the adapters already swallow a blip (Dathost retries a 5xx three times inside one call), so a probe that failed here failed long enough to matter. The nodes provider is unhealthy only when **every** enrolled node is off the wire: one venue box off on a Tuesday is capacity news (`available: 0`, `fleet.node_disconnected`), not a provider outage, and no node enrolled at all is healthy-and-empty. `fleet.node_disconnected` (T12), `fleet.orphan_found` (the reaper) and `fleet.budget_threshold` (T5) were already said where they happen; what they lacked is **where they go**. `@ezpug/match-api` **0.8.0**, additive: `ApiKey.fleetWebhook`, `PUT /v1/keys/:keyId/fleet-webhook`, `fleetWebhook` at the mint, and `ledgerFilterSchema.since`. A key that registers a fleet webhook hears its four capacity facts on that one endpoint instead of on every open match's callback — same envelope, same `matchId`/`seq`, same signature, the events route still replays it; the decision is one lookup in `machine.emit` and falls back to the match's callback when the key or its secret is gone, because a delivery to a known endpoint beats none. A `secretId` the key never registered is refused: a `kid` no verifier knows fails silently at three in the morning. The T2 schema had the columns since day one, so **no migration**; the duplicated `fleetWebhookUrl`/`fleetWebhookSecretId` on `KeyRecord`/`AuthenticatedKey` collapsed into the contract's one field. `?since=` is the window a **bill** is asked over, not the window a row was born in (open now, or released at or after it — the budget's own read), so a server allocated before midnight and still running is part of tonight's; documented in `docs/operations.md` with the one-query `jq` sum. `pnpm verify` green (24/24; 456 TS + 2 skipped, C# included), `pnpm verify:extended` green (exit 0, `@ezpug/match-api@0.8.0 is publishable`); the `EZPUG_CS2_TESTS` lane skipped — the dev CS2 container is down and nothing here reaches a server. 0.8.0 is released in the tree, not published: the registry door is T38a's. — 4f2d69b — 2026-09-07
T32: fifty matches with fault injection, and the six questions asked of the wreckage. One world holds the night (`apps/orchestrator/src/faults.extended.test.ts`): the orchestrator over memory on a **fake clock**, `/link` and `/node` on a real port, and all three providers at once — the simulator, the **fake Dathost** with a template this suite builds through the vendor's own multipart create, and a **fake node** whose containers' plugins dial `/link` with the token the provider planted (`ezpug.json` for a clone, the `start` frame for a container). Fourteen scenario kinds, one of each guaranteed and the rest drawn by weight, shuffled by one seed: allocation refused, a boot that never ends, a crash with a restorable backup and one without, duplicate and out-of-order link frames, an agent that disconnects mid-match, the vendor's API down for a minute, a webhook endpoint failing ten times, a key at its budget. After the fifty: every ledger row closed, every provider's `list()` empty, every match terminal **with a reason** (and the set of reason kinds pinned, so a scenario that starts passing for the wrong reason is still red), the events route replaying every accepted webhook byte-for-byte, no GSLT leased, each key's month equal to the sum of `accruedCents` over its rows. **Three traps, all of them the fake clock's.** (1) Nothing moves unless the test moves it, so `await`ing a provider call that sleeps on its own backoff — the reaper listing a vendor that is answering 500 — is a deadlock: `alongside()` advances while it waits. (2) The fakes have no timers, so the driver speaks for them: `world.tick()` heartbeats the agent and every connected plugin **before** every advance, because the node link drops an agent silent for two heartbeat intervals and the next `lan` request then finds `no LAN node has free capacity` (the first red run, and it took a registry dump to see). (3) A heartbeat has to *land* before the clock moves — write, settle, then advance — since the frame crosses a real socket while `advance()` runs in microtasks. **Two findings.** A webhook endpoint that fails reorders what a client receives: the retried envelopes arrive behind the ones that went out while they were failing, so the assertion is on `GET /v1/matches/:id/events`, which is the order — the honest reading of the PRD's line and the reason the route exists. And **T32a**, a new PRD task: a container the agent was holding when it went away is still running when it dials back, because `adopt()` reads *open* ledger rows and that match's row was closed while the agent was gone — neither adopted nor listed, so nothing stops it and the reaper cannot see it; the fix is a decision about what an agent's snapshot means, not a patch, and the assertion that earns it is named in a comment where it will go. `pnpm faults --seed <seed>` (`scripts/faults.mjs`) is the reproduction door; the suite also honours `EZPUG_IRON_FAULT_SEED`. Green on four seeds. `pnpm verify` green (24/24, 465 TS + 2 skipped, C# included), `pnpm verify:extended` green (exit 0), the fault suite 3–5 s in both tiers; the `EZPUG_CS2_TESTS` and `EZPUG_DATHOST_TESTS` lanes skipped with their printed reasons — nothing here touches a real server. — dfdfd1e — 2026-09-07
T32a: the container T32 found still running on the node, and the decision that ends it. **The ledger row is the warrant.** A container an agent reports that this process does not hold is asked one question — what does the ledger say about it? — and the answer has three shapes: an **open** row of this deployment is `adopt()`'s (unchanged); a **closed** row of this deployment is a container whose life is over, so it is stopped; **no row of this deployment at all** is never touched. The third one is the whole reason this is a decision rather than a patch: `nodes` is not deployment-scoped (T21c), so a box re-enrolled from a dev orchestrator to production still runs the dev world's containers and a restored database is a row that no longer exists — a container this process cannot account for is one it has no business killing, so it is warned about once (naming the deployment it looked in and what an operator does about it) and left occupying the capacity slot it really occupies. No grace window is owed the second case, and that is the argument the reaper's own two minutes makes for me: its grace exists to cover a row that has not caught up with an allocation, and a row that has already been *closed* cannot be that one. Two things make the sweep hold up in the small: it runs on **every** snapshot a node sends rather than only on connect, so a `stop` the node did not apply is re-sent (idempotent by contract) and the retry policy is the sweep itself; and a `stop` this process actually **wrote** is remembered against the socket it went out on (`told`), so the ordinary path — deallocate writes a stop, a snapshot older than the frame arrives — is not mistaken for a stray, while a stop with no socket to be written on, or one written on a socket that then died, is still owed. Verdicts are cached per container id because they cannot change: a closed row is never reopened, and an id no row of ours names is one no row of ours will ever name. Three tests, each checked red against the old code: the T32a scenario end to end (the agent leaves, the match is ended while it is gone, the container is still there and no `stop` was written, it dials back and the container goes), the foreign container left alone and said once however many snapshots repeat it, and T32's own `leaves nothing of ours running at any provider` grown the assertion its comment promised — `rig.node.instances()` empty, which is **three** stray containers before the fix and none after. `docs/nodes.md` (both the orchestrator's half and the "when something is wrong" list) and `docs/operations.md` (under the reaper, because this is exactly what the reaper structurally cannot see: a provider's `list()` is what the process holds in memory) carry the rule. `pnpm verify` green (24/24, 467 TS + 230 C#) and `pnpm verify:extended` green (exit 0, including the conformance suite against the real orchestrator, the fault suite and the 0.8.0 publish check); the CS2 and Dathost lanes skipped with their printed reasons — no dev node was up this run, and this fix is proved against the fake node rather than on a real box. — fbe2f10 — 2026-09-07
T33: `apps/cli` — `ezpug-iron`, one call on the typed client per verb and nothing that skips the API: `keys create|list|revoke`, `gamemodes list`, `matches create|list|get|watch|cancel|command`, `servers list|kill|console`, `nodes enrol-token|list|drain`, `budget`, `dathost image --check|--build`. `matches list` is beyond the PRD's line and is there because `matches get <id>` is useless to an operator who cannot find the id. **Create takes a document, not thirty flags** (`--file <path|->`, patched by the two things a rerun actually re-decides and parsed against `matchRequestSchema` on this box before anything goes out) — a flag surface over rosters, map plans and callbacks would be a second, worse schema that drifts from the first. `matches command` maps flags to fields from one table per command type and lets Zod be the judge. **Three rules hold across the whole surface**: `--json` is a property of the writer (`output.ts`), so the human channel is a table and the machine channel is one document — one per *line* for `watch`, and under `--json` that pipe carries stream frames and **nothing else**, the close being the exit code's to report so `| jq` never meets a line that is not a frame. A secret leaves by one door once (`reveal` and `emit`), every other line going through a redactor; **no secret is ever a flag** — `EZPUG_IRON_API_KEY` is environment-only, the same rule T18 keeps for the Dathost password, because a flag is in the shell history and in `/proc` while the command runs, and a test sweeps every stream of every run for the key. Exit codes a script branches on: 0 / 1 (the orchestrator said no — including a *rejected* command, which is a 200 on the wire and still did not happen) / 64 / 69, and a refusal is never a 69 because `budget_exceeded` is money, not weather. `dathost image` is the one verb that is not HTTP: a thin pass-through to `scripts/dathost-image.mjs` found by walking up out of the module and the cwd, so there is one place where "what the template must hold" is decided; outside a checkout it says so and exits 69. **A real bug the in-process tests could not see**: `process.exit()` tears a process down without flushing stdout, and stdout to a pipe is queued in 64 KB chunks — `pnpm --silent iron keys list --json | …` against the dev orchestrator handed me a document that stopped mid-string at byte 65 536. `main.ts` drains both streams first now, and `main.test.ts` spawns the real process through a real pipe with 250 keys behind it to keep it drained. Also found and fixed: `.gitignore`'s `bin/` (there for .NET's build output) was swallowing `apps/*/bin/*.mjs`, so both this command's wrapper and the node agent's — files their `bin` fields have named since T11 — were in no clone; `!apps/*/bin/` un-ignores them and `apps/node/bin/ezpug-node.mjs` is committed here with it. Tested against the fake orchestrator on a real port through `runCli` exactly as a terminal drives it (50 tests, the stream over a real ws upgrade, the T18 script's own `--help` actually invoked), and **driven live** against the dev orchestrator on this box: a `pug` on the sim created, read, watched to `stream closed 4000 — the match reached a terminal state`, the ledger and the budget read, a command on an ended match refused `invalid_state` with exit 1, and every key minted for the check revoked afterwards — the ledger shows the row released and nothing left running. `pnpm verify` green (27/27, 467 TS + 236 C#, `turbo boundaries` clean). — f9bc790 — 2026-09-07
T34: CI and the release pipeline — five tags, five artifacts, one definition of green. `verify.yml` (from PRD-01) grew a `workflow_call` trigger and is now the *gate every release calls*: `images.yml` and `plugins.yml` name it with `uses:`, so a tag runs exactly what a pull request ran and nothing publishes off a red tree. **The tag is the version.** An image has no manifest in the tree carrying a number, so inventing a second home would only have created a drift to check for; instead `scripts/release-image.mjs` (`pnpm release:image plan <tag>`) is the one table a tag is read against — which Dockerfile, which platforms, which registry tags — and the workflow parses no tag itself. What ties an image to this repo is its `org.opencontainers.image.revision` label (the commit, passed as `EZPUG_GIT_SHA` because `.git` is not in the build context) and the build provenance `actions/attest-build-provenance` signs against the manifest list, the way npm provenance signs the package. **Multi-arch where it is cheap means native, not emulated**: `orchestrator@` and `node@` build `linux/amd64` on `ubuntu-latest` and `linux/arm64` on `ubuntu-24.04-arm` (free on a public repo), each pushed **by digest** with its own gha cache scope, and one merge job writes the human-readable tags as a manifest list over the two digests — so a half-published multi-arch tag cannot exist even when one architecture fails; a qemu arm64 `pnpm install` would have been an hour per release. `cs2@` is `linux/amd64` alone: Valve ships the dedicated server and its steamrt base for nothing else, and an arm64 tag would be an image with no game in it. A release publishes `x.y.z`, the moving `x.y` and `latest`; a prerelease publishes its exact version only, because `latest` is a promise about a release. **A version the registry already serves is refused, never overwritten** (`imagetools inspect` in the plan job) — re-releasing is a new patch. `plugins@x.y.z` runs `plugins/publish.sh`, zips its `addons/` tree (`ezpug-plugins-x.y.z.zip`, unpacks into a server's `game/csgo/`) and attaches it to the tag's GitHub release with its sha256 in the notes; the tag must name the version `plugins/EZPug.Core.csproj` carries, and the check reads it out of the `build.json` **in the tree that was just built**, so the artifact is its own witness. `workflow_dispatch` is the dry run on both (same build, no push, no upload). `docs/pins.md` grew "The images we publish" — names, platforms, who pins one (the platform's `EZPUG_IRON_IMAGE`, production's `compose.prod.yaml`, a node's two) — and `scripts/check-pins.mjs` now imports the release table and fails `pnpm lint` when an image or a platform is missing from it (checked red). `docs/operations.md` has "Releasing: CI and the tags", the README a `## Releasing` section for a stranger. `apps/orchestrator/src/release.test.ts` runs the plan script the way the workflow does and holds the workflows, the pins table, the runbook and the README against it (11 tests). Verified beyond the unit tier: the zip step replayed locally end to end (build → `build.json` → tag check → zip → sha256, 17 files) and the `imagetools create` argument loop and release notes with it; the dry-run image build run for real (`docker buildx build --provenance=false --output type=cacheonly`, the dispatch path's exact flags, green in 47 s). The pipeline's push half is provable only by pushing a tag, which this task does not do — no artifact was published. `pnpm verify` green (27/27, 478 TS + 2 skipped, 236 C#); the CS2 and Dathost lanes are untouched by this task. — 243e135 — 2026-09-07

T35: deploy — `gs.ezpug.com` is live and its smoke is green. `compose.prod.yaml` is its own project (`ezpug-iron`, containers `ezpug-iron-prod-*`) with its own Postgres and Redis on named volumes published to nobody, one one-shot `migrate` the orchestrator waits on, and the service on `172.17.0.1:3431` and nothing else — no CS2 on this box, per the hard don't. `docker/traefik/ezpug-iron.yml` is the front door with a project-named `ezpug-iron-redirect` (the file provider merges that directory into one namespace, so the neighbours' `redirect-to-https` would be a collision), and the sockets need no middleware: the link, the node link and the stream heartbeat every 10 s, well inside Traefik's 180 s idle. `scripts/deploy.sh` is preflight → build → **dump then** migrate → up → routes → smoke, plus `backup`, `key` and `rollback`; preflight is where the drift dies (hostname and port against the route file, rails naming compose services and not this box, the Dathost trio when `dathost` is a provider, a backup directory outside the checkout, `NODE_ENV=production` and no dev door). Two things the task decided rather than inherited. **The image comes from one variable**: unset, the deploy builds this checkout and rotates `:previous` for `rollback`; set to a published tag, `build` *pulls* and `rollback` refuses and tells you to bump the line — a released tag can never be silently overwritten by a local build. **The first key needs a door production does not otherwise have**: the bootstrap key is refused under `NODE_ENV=production` and `keys create` needs an `admin` key already, so `mint-key.ts` is a third tsdown bundle in the image and `deploy.sh key` runs it once; every key after it is the CLI's. The smoke feeds that key to curl on stdin (`--config -`), never argv. **One real bug, found by being the first thing to ever run `dist/migrate.mjs`**: the bundle sits at `/app/dist/` and `MIGRATIONS_FOLDER` walks two levels up to `/drizzle`, so the first production migration died on `Can't find meta/_journal.json` — `resolveMigrationsFolder(env)` now reads `EZPUG_IRON_MIGRATIONS_DIR` (which the image has always set for the service), with its own unit test. Proven on the box: full `./scripts/deploy.sh` twice, seven smoke checks green (`/healthz` on the bridge and over real TLS, GET→301 to https, `/v1/capacity` 200 with the operator key and 401 without), a Let's Encrypt certificate on first ask, two rotated dumps in `/var/backups/ezpug-iron`. `ralph/DEPLOY.md` is the runbook (first deploy, backups-are-credentials, rollback, the Saturday); `docs/operations.md` grew a Deploying section and `docs/pins.md` is honest about where the image is pinned. `pnpm verify` and `pnpm verify:extended` green. Blocked note on the PRD: no Dathost credentials on this box, so production runs `sim,nodes` until the owner adds the trio. Re-deployed from the clean tree after the commit, so production runs a labelled, committed revision and `:previous` holds the build before it. — 1542982 — 2026-09-07

T37: the LAN rehearsal — this box as a venue, against **production**, with nothing simulated but the venue. `saarlan-rehearsal` enrolled through `gs.ezpug.com` and dialling `wss://gs.ezpug.com/node` over real TLS from the `ghcr.io/ezpug/ezpug-iron/node:dev` container (max 2 instances, warm 0, the 67 GB game volume, the docker socket), driven end to end by `ezpug-iron` alone. **`pnpm node:build` did not build at all** and that was the first finding: `docker/node/Dockerfile` still ran `pnpm --filter=@ezpug/node... build`, and `@ezpug/node...` reaches the manifests through `@ezpug/match-api` while the kit that builds their widgets is a devDependency of the manifests and a peer of the published package — the edge pnpm counts as a cycle and refuses to order (T25's finding, fixed then for the orchestrator's image and not for this one). Now `pnpm exec turbo run build --filter=@ezpug/node...`, and both images rebuilt at b5980f2 for the run. **Match A**: a `lan` `pug` with ten bots landed on the node twenty seconds after the request (cold CS2 boot), was `live` at +40 s and `ended` six minutes later on four rounds and a side swap — 49 events on the match's route, 50 webhook deliveries all signature-verified against the registered secret, four `backup_written`, the ledger row `released` at €0.00, no container left. **Drain** left it alone and refused the next `lan` request `no_capable_server: no LAN node has free capacity` with exit 1; `--undrain` took work again. **Match B, `docker kill` mid-match with the node undrained**: `match.recovering` 30 s later ("silent for 30000 ms; nodes reports the server gone", `backupRound` 3), the walk came back to the same node, and 38 s from the kill there was a replacement container whose plugin had loaded `matchzy_…_round02.json` and announced `match.server_ready` with `restored`. **And then it did not finish** — MatchZy resumed from the checkpoint by its own log ("starting match with score 1:1 after round 2") but never said `going_live` a second time, that event belonging to the start of a series, and `going_live` is the only thing the recovery window closes on; the API's `unpause` is refused outside `live` so only `rcon` reaches the pause MatchZy takes after a restore; twenty minutes later the match was `failed: server_lost` — "no going_live within 1200000 ms of the replacement being ready" — while the server was up and playing. Both rows closed, the container stopped and removed: the ledger and the clean-up are right, the outcome is not. T14 proved this path against the sim and a fake node, both of which *script* the `going_live` real MatchZy withholds, which is exactly why only hardware could find it — written up as **T37a**. **Match C, the same kill on a drained node**: `match.recovering` → `match.failed` `{kind: server_lost, detail: "no server to restore onto: no_capable_server"}` in 30 s, which is the honest answer the PRD predicted for a venue with one box. **Un-enrol**: `DELETE /v1/fleet/nodes/:id` closed the live socket `4009` and every dial after it was `4001` (the row is gone) — and `--restart unless-stopped`, which the runbook's own step 4 prints, then restarted the agent eight times in a minute, spending its "not dialling again" decision once per restart, because no docker restart policy can tell an exit-2 decision from a crash. Three terminal edges cost time and are **T37b**: `keys create` cannot register a webhook secret while every request must name one (so the run's key was minted through `POST /v1/keys`), there is no `nodes` verb for un-enrolling, and `pnpm iron` resolves a relative `--file` against `apps/cli`. Also swept: three busybox containers `dockerode.test.ts` left behind on interrupted runs (35 hours old; the sweep is T37b's). Docs corrected by what happened: `docs/nodes.md` (the restart-policy trap, the two close codes, the measured recovery numbers, and a new "What a rehearsal found"), `docs/operations.md` (Recovery's hardware paragraph, the CLI's three edges). T36 is `> blocked:` again — `.env.production` still has the Dathost trio commented out and `sim,nodes` as its providers, so T37 was taken out of order; the note hands it the two things this run learned about talking to production. `pnpm verify` green (27/27, 500 TS + 2 skipped, 236 C#) and `pnpm verify:extended` green twice, CS2 and Dathost lanes skipped with their printed reasons. Nothing of the rehearsal is left: no node, no container, no volume, both keys revoked. — 382c9a5 — 2026-09-07
